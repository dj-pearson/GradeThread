
=== 2026-07-02 (loop): US-1544 (AI group-boundary verification) ===
- NEW edge lib ai-group-verify.ts: sampleGroupsForVerify (first/middle/last per group -
  boundary shots tell the story, cost stays O(groups); takes groups in order until the
  40-photo budget, reports truncated + groupsCovered), ONE vision call with photos labeled
  G<n>-P<n> + a suggest_group_fixes tool (merge/split/move, confidence, reason), and
  normalizeGroupSuggestions (numbers/labels -> caller ids; drops malformed/oob/self-refs;
  clamps confidence). Runs under the US-1065 runActionCascade: lightweight model first,
  escalates only when the cheap pass cannot produce a parseable suggestions array.
  enterAiFeature(autolister_verify_groups) for US-894 spend attribution. <2 coverable
  groups short-circuits with model:none (no AI call, not metered).
- NEW route POST /api/flipdesk/autolister/verify-groups: ordered groups of staged
  storage paths; EVERY path checked against ${ownerId}/_staging/ BEFORE any DB/AI work
  (403 on foreign; stricter than classify-photos ownerId/ prefix per AC); groups capped at
  MAX_BATCH_ITEMS; requireFlipdesk autolister + checkQuota; increment_ai_actions only when
  a vision call ran; falls under the shared autolister POST limiter (20/min). Writes
  NOTHING.
- tenant-isolation_test.ts: new live-gated case - B posting a foreign _staging path to
  verify-groups must be denied (401/402/403/404).
- Web autolister.tsx: Verify groups button in the group toolbar (1 AI action, disabled
  <2 groups) + SILENT auto-run right after auto-group (passes the just-created groups
  explicitly - state not committed yet - and only toasts when something was flagged; the
  degenerate-grouping branch skips it since the user is told to undo). Suggestions render
  as amber dismissible chips on the affected group (move chips on the SOURCE group only)
  with reason tooltip + confidence %; Apply routes through the US-1543 undoable mutations
  (merge -> mergeGroups, move -> movePhotos, split -> pull photos + new group), so every
  applied suggestion is one-click undoable; nothing is ever auto-applied.
TESTS: 5 new deno tests (sampling passthrough/budget-truncation/empty-group skip;
normalization mapping/clamping + malformed/oob/self-ref drops). FULL edge suite 2464/0/70;
web 1114/1114; tsc -b green; eslint clean; npm run build green (47 pages).
Still LOCAL-ONLY; queues behind held migration 00339.
