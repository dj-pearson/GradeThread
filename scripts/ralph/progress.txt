# Ralph Progress Log
Started: 2026-02-16
Branch: ralph/gradethread-build
---

## Foundation (US-001 through US-021)
- Status: COMPLETE (pre-built before Ralph)
- 21 stories covering: Vite scaffold, Tailwind/shadcn, TypeScript types, Supabase client, auth layer, routing, layouts, dashboard shell, all pages, static assets, DB migration, edge functions, CF config
- Notes: Built manually in initial foundation session. All typecheck and build passing.
---

## US-022: Database migration: add inventory and financial tracking tables
- Status: COMPLETE
- Files changed: supabase/migrations/00002_inventory_financial.sql, prd.json, scripts/ralph/prd.json
- Notes: Created migration with 2 new enums (item_status, listing_platform), 4 new tables (inventory_items, listings, sales, shipments). All tables have UUID PKs, proper FK constraints (CASCADE on delete, SET NULL for optional refs), indexes on all FKs and status columns, RLS policies enforcing ownership through the inventory_items -> user_id chain, and set_updated_at triggers on tables with updated_at columns. Sales table intentionally has no updated_at (immutable records). Shipments RLS uses a JOIN through sales to inventory_items for ownership verification.
- Timestamp: 2026-02-16
---

## US-023: TypeScript types for inventory and financial tables
- Status: COMPLETE
- Files changed: src/types/database.ts, prd.json, scripts/ralph/prd.json
- Notes: Added ItemStatus (8 values) and ListingPlatform (8 values) enum types. Created Row, Insert, and Update types for inventory_items, listings, sales, and shipments tables. Updated Database interface with 4 new table entries in public.Tables and 2 new enum entries in public.Enums. All nullable SQL columns mapped to `T | null`. Insert types have optional fields for columns with DB defaults. Sales has no updated_at (immutable records per migration design). Typecheck and build pass clean.
- Timestamp: 2026-02-16
---

## US-024: Database migration: admin tables and roles
- Status: COMPLETE
- Files changed: supabase/migrations/00003_admin_tables.sql, src/types/database.ts, prd.json, scripts/ralph/prd.json
- Notes: Created user_role enum (user/reviewer/admin/super_admin), added role column to users table (default 'user'). Three new tables: admin_audit_log (for tracking admin actions), human_reviews (for reviewer overrides on low-confidence grades), ai_prompt_versions (for tracking AI prompt iterations and accuracy). Created is_admin() and is_reviewer_or_admin() SECURITY DEFINER helper functions for RLS. RLS policies: audit log admin-only, human reviews admin+reviewer, prompt versions admin-only (full CRUD). Added indexes on all FKs, action/target_type columns, and users.role. TypeScript types updated with UserRole enum, role on UserRow/UserInsert, and Row/Insert/Update types for all 3 new tables plus Database interface entries.
- Timestamp: 2026-02-16
---

## US-025: TypeScript types for admin tables
- Status: COMPLETE
- Files changed: scripts/ralph/prd.json, prd.json
- Notes: All acceptance criteria were already satisfied by the US-024 implementation which added UserRole enum, UserRow.role field, Row/Insert/Update types for admin_audit_log, human_reviews, and ai_prompt_versions, plus Database interface entries and UserRole enum registration. Verified typecheck and build pass. No code changes needed — only prd.json updates to mark story as passing.
- Timestamp: 2026-02-16
---

## US-026: Image upload utility with Supabase Storage
- Status: COMPLETE
- Files changed: src/lib/storage.ts, prd.json, scripts/ralph/prd.json, scripts/ralph/progress.txt
- Notes: Created storage utility module with three exported functions: uploadSubmissionImage (validates file type/size, uploads to {userId}/{submissionId}/{imageType}_{timestamp}.{ext}), getImageUrl (creates signed URLs with 1hr expiry), deleteSubmissionImage (removes files from bucket). Validates against MIME types (jpeg/png/webp) and 10MB size limit matching the DB bucket config. All errors throw with meaningful messages. Typecheck and build pass clean.
- Timestamp: 2026-02-16
---

## US-027: Client-side image compression and validation
- Status: COMPLETE
- Files changed: src/lib/image-utils.ts, prd.json, scripts/ralph/prd.json, scripts/ralph/progress.txt
- Notes: Created image-utils module with two exported functions: validateImage(file) checks MIME type (jpeg/png/webp), file size (10MB max), and resolution (min 1200x1200px), returning structured validation errors with user-friendly messages. compressImage(file, maxWidth, quality) uses canvas API to resize images (default max 2400px wide), compress (default 0.85 quality), and correct EXIF orientation by parsing the orientation tag from the first 64KB of JPEG files. Handles all 8 EXIF orientation values including dimension swaps for rotated images. Outputs WebP for JPEG/WebP inputs, preserves PNG format. Typecheck and build pass clean.
- Timestamp: 2026-02-16
---

## US-028: Edge function: JWT auth middleware
- Status: COMPLETE
- Files changed: services/edge-functions/src/middleware/auth.ts, services/edge-functions/src/main.ts, prd.json, scripts/ralph/prd.json, scripts/ralph/progress.txt
- Notes: Created Hono middleware that extracts Bearer token from Authorization header, validates it via supabase.auth.getUser(), and sets user and userId on the Hono context. Returns 401 JSON error for missing or invalid tokens. Applied to /api/grade/* routes in main.ts using app.use() path matching. Health and webhook routes remain unprotected as required. Uses createMiddleware from hono/factory with typed Variables for user and userId.
- Timestamp: 2026-02-16
---

## US-029: Edge function: rate limiting middleware
- Status: COMPLETE
- Files changed: services/edge-functions/src/middleware/rate-limit.ts, services/edge-functions/src/main.ts, prd.json, scripts/ralph/prd.json, scripts/ralph/progress.txt
- Notes: Created rateLimiter(maxRequests, windowMs) factory function that returns Hono middleware. Uses in-memory Map keyed by userId from auth context. Tracks request count and window reset time per user. Returns 429 with Retry-After header and JSON error body when limit exceeded. Includes periodic cleanup of expired entries (every 5 minutes). Adds standard X-RateLimit-Limit, X-RateLimit-Remaining, and X-RateLimit-Reset headers to all responses. Applied to /api/grade/* routes after auth middleware with default 60 requests per 60 seconds. Skips rate limiting if no userId is available in context.
- Timestamp: 2026-02-16
---

## US-030: Photo upload UI component with drag-and-drop
- Status: COMPLETE
- Files changed: src/components/submission/photo-upload.tsx, prd.json, scripts/ralph/prd.json, scripts/ralph/progress.txt
- Notes: Created PhotoUpload component with 8 upload slots (front, back, label, detail required; detail 2, defect 1-3 optional). Each slot shows icon (Shirt/Tag/Search/AlertTriangle from lucide-react), label, required indicator, and description. Supports drag-and-drop (onDragOver/onDrop) and click-to-select (hidden file input). Shows image preview with hover-reveal remove button. Validates via existing validateImage (type/size/resolution) and compresses via compressImage before emitting. Emits onChange with PhotoUploadItem[] ({file, imageType, preview}). Responsive grid: 2 cols mobile, 4 cols desktop. Used Map<string, SlotState> instead of Record for strict TypeScript compatibility. Processing spinner shown during validation/compression.
- Timestamp: 2026-02-16
---

## US-031: Submission wizard: step 1 - garment info form
- Status: COMPLETE
- Files changed: src/components/submission/garment-info-form.tsx, src/components/ui/select.tsx (shadcn), src/components/ui/textarea.tsx (shadcn), prd.json, scripts/ralph/prd.json
- Notes: Created GarmentInfoForm component with garment type select (GARMENT_TYPES), category select (GARMENT_CATEGORIES filtered by type via CATEGORY_BY_TYPE mapping), brand input (optional), title input (required, max 100 chars with counter), description textarea (optional, max 500 chars with counter). Form validation shows inline error messages for required fields. onSubmit passes validated GarmentInfo to parent. Supports defaultValues prop for state persistence. Added shadcn select and textarea components via npx shadcn@latest add.
- Timestamp: 2026-02-16
---

## US-032: Submission wizard: multi-step container with progress
- Status: COMPLETE
- Files changed: src/pages/new-submission.tsx (new), src/routes/index.tsx, prd.json, scripts/ralph/prd.json
- Notes: Created 3-step submission wizard at /dashboard/submissions/new. StepIndicator component shows numbered circles with checkmarks for completed steps and connecting lines. Step 1 renders GarmentInfoForm with defaultValues for state persistence when navigating back. Step 2 renders PhotoUpload with Back/Next buttons (Next disabled until all 4 required photos uploaded). Step 3 shows review with garment details grid, photo thumbnails grid, and pricing info from user's plan. All state (garmentInfo, photos) persisted in parent component via useState so navigating back preserves data. Route added to router inside protected dashboard layout.
- Timestamp: 2026-02-16
---

## US-033: Submission creation API endpoint
- Status: COMPLETE
- Files changed: services/edge-functions/src/routes/grade.ts, supabase/migrations/00004_increment_grades_function.sql, prd.json, scripts/ralph/prd.json
- Notes: Fully implemented POST /api/grade/submit endpoint with multipart form data parsing. Validates garment_type, garment_category, title (required), and image files (requires front, back, label, and at least 1 detail image). Checks user's grade usage against plan limits (free:5, starter:50, professional:500, enterprise:unlimited) with monthly reset logic. Creates submission record with status 'pending', uploads each image to Supabase storage under {userId}/{submissionId}/{imageType}_{timestamp}.{ext}, creates submission_image records with display_order. Increments grades_used_this_month via Postgres RPC function (new migration 00004) or resets counter if month rolled over. Returns 201 with { submissionId, status } on success, 403 with upgrade message if limit exceeded, 400 with detailed validation errors for invalid input. Also implemented GET /status/:id endpoint scoped to authenticated user. Includes cleanup logic on failure (deletes uploaded images and submission record).
- Timestamp: 2026-02-16
---

## US-034: Claude Vision API integration for per-image analysis
- Status: COMPLETE
- Files changed: services/edge-functions/src/lib/ai-grading.ts (new), prd.json, scripts/ralph/prd.json
- Notes: Created ai-grading.ts module exporting analyzeImage(imageUrl, imageType, garmentType) function and typed interfaces (PerImageAnalysis, DetectedIssue, ConditionSignal, FactorScores). Uses Anthropic SDK v0.24.1 with base64 image source (parseImageInput helper handles data URI and raw base64 formats). System prompt establishes expert clothing condition assessor role with grading scale and 5 weighted factors. User prompt includes image-type-specific context (front/back/label/detail/defect) and garment-type-specific criteria (tops/bottoms/outerwear/dresses/footwear/accessories). Requests structured JSON with detected_issues[], condition_signals[], and estimated_scores per factor. Response parsing strips markdown code fences, validates structure, and clamps scores to 1.0-10.0 range. Error handling covers timeouts, rate limits, and parse failures with contextual error messages. Logs API usage (input/output tokens, latency) for monitoring.
- Timestamp: 2026-02-16
---

## US-035: Claude Vision API: composite grading from all images
- Status: COMPLETE
- Files changed: services/edge-functions/src/lib/ai-grading.ts, prd.json, scripts/ralph/prd.json
- Notes: Added compositeGrade(perImageResults[], garmentInfo) function to ai-grading.ts. Exports new types: CompositeGradeResult, GarmentInfo, DefectFound. Sends all per-image analyses as structured JSON input to Claude with comprehensive composite system prompt including full grade tier definitions (NWT through Salvage with score ranges and descriptions). Applies factor weights (fabric 30%, structural 25%, cosmetic 20%, functional 15%, odor 10%) and recalculates weighted average server-side for authoritative scoring. Rounds overall_score to nearest 0.5 increment. Derives grade_tier deterministically from calculated score via scoreToGradeTier(). Validates and clamps all factor scores (1.0-10.0), confidence (0.0-1.0). Sets needs_human_review flag when confidence < 0.75. Includes prompt_version field ("composite_v1") referencing ai_prompt_versions concept. Validates defects_found array entries for required fields and valid severity values. Comprehensive error handling for timeouts, rate limits, and parse failures with API usage logging.
- Timestamp: 2026-02-16
---

## US-036: Grade calculation and report creation pipeline
- Status: COMPLETE
- Files changed: services/edge-functions/src/lib/grading-pipeline.ts (new), prd.json, scripts/ralph/prd.json
- Notes: Created processSubmission(submissionId) orchestration function that runs the full grading pipeline. Fetches submission record and validates status is 'pending', sets status to 'processing'. Fetches associated submission_images, downloads each from Supabase Storage, converts Blob to base64 data URI with correct MIME type detection from file extension. Runs analyzeImage() on all images in parallel via Promise.all. Runs compositeGrade() with per-image results and garment info. Creates grade_report record with all factor scores, AI summary, confidence score, model version, and a UUID certificate_id generated via crypto.randomUUID(). Builds detailed_notes object from per-image issue/signal summaries plus defects summary. Updates submission status to 'completed' on success. Full try/catch wraps entire pipeline — on failure, logs error and updates submission status to 'failed' (with nested try/catch to handle status update failures). Comprehensive console logging at each pipeline stage with timing metrics.
- Timestamp: 2026-02-16
---

## US-037: Trigger grading pipeline on submission creation
- Status: COMPLETE
- Files changed: services/edge-functions/src/routes/grade.ts, services/edge-functions/src/lib/grading-pipeline.ts, prd.json, scripts/ralph/prd.json
- Notes: Updated POST /api/grade/submit to set status to 'processing' after image upload and trigger processSubmission() as fire-and-forget with .catch() error handler. Response now returns status 'processing' instead of 'pending'. Updated GET /api/grade/status/:id to return { id, status, grade_report, created_at, updated_at } — fetches from grade_reports table when status is 'completed', returns null otherwise. Updated processSubmission() to accept both 'pending' and 'processing' statuses (since submit endpoint now sets 'processing' before calling the pipeline). Typecheck and build pass clean.
- Timestamp: 2026-02-16
---

## US-038: Submission detail page with grade report display
- Status: COMPLETE
- Files changed: src/pages/submission-detail.tsx, src/components/ui/skeleton.tsx (shadcn), src/components/ui/progress.tsx (shadcn), prd.json, scripts/ralph/prd.json
- Notes: Fully rebuilt submission-detail.tsx from placeholder to complete grade report page. Features: loading skeleton while fetching, garment info card (title/brand/type/category), large circular overall score with color-coded tier badge (green >7, yellow 5-7, red <5), factor breakdown with 5 labeled progress bars showing weights, AI analysis summary card, confidence indicator (high/medium/low with icons), detected issues as badges or bullet points from detailed_notes, image gallery with signed URLs from Supabase Storage, share certificate button linking to /cert/:certificateId, status-aware states (processing spinner, failed error, pending info). Added shadcn skeleton and progress components. Used type assertion for submission_images query due to tsc -b type inference resolving data to 'never' despite correct Database interface definition.
- Timestamp: 2026-02-16
---

## US-039: Submissions list page with filtering and pagination
- Status: COMPLETE
- Files changed: src/pages/submissions.tsx, src/components/ui/table.tsx (shadcn), prd.json, scripts/ralph/prd.json
- Notes: Replaced placeholder submissions page with full implementation. Table displays title, brand, status badge (color-coded), grade score (with color), and date submitted. Filter controls for status (all/pending/processing/completed/failed/disputed) and garment type (all 6 types). Sort by date (default newest first) or grade with toggle direction. Pagination with 20 items per page, page number display, previous/next buttons. Click row navigates to /dashboard/submissions/:id. Empty state with CTA button to create first submission. Loading skeleton while fetching. Uses TanStack Query with queryKey including all filter/sort/page params and 5-minute stale time. Fetches grade reports for completed submissions in a second query and merges results. Type assertions used for Supabase query results due to tsc -b type resolution issue.
- Timestamp: 2026-02-16
---

## US-040: Stripe checkout session creation for grade payments
- Status: COMPLETE
- Files changed: services/edge-functions/src/routes/payments.ts (new), services/edge-functions/src/main.ts, scripts/ralph/prd.json, prd.json
- Notes: Created new payments route file with POST /checkout-session endpoint. Accepts { submissionId, tier } JSON body. Three tiers: standard ($2.99/299 cents), premium ($7.99/799 cents), express ($12.99/1299 cents). Validates tier is valid, validates user owns the submission via Supabase query. Creates Stripe Checkout Session with mode:payment, price_data with product name/description, and metadata containing submission_id, user_id, and tier for webhook processing. Success URL redirects to /dashboard/submissions/:id?payment=success, cancel URL to /dashboard/submissions?payment=cancelled. Returns { sessionId, url }. Registered route in main.ts at /api/payments with auth middleware applied.
- Timestamp: 2026-02-16
---

## US-041: Stripe webhook handling for payment events
- Status: COMPLETE
- Files changed: services/edge-functions/src/routes/webhooks.ts, scripts/ralph/prd.json, prd.json
- Notes: Replaced stub webhook handler with full Stripe webhook implementation. Verifies webhook signature using stripe.webhooks.constructEvent with STRIPE_WEBHOOK_SECRET env var. Handles 4 event types: (1) checkout.session.completed — reads submission_id/user_id from session metadata, marks submission payment_status as paid, triggers grading pipeline fire-and-forget; (2) customer.subscription.created/updated — finds user by stripe_customer_id, maps subscription to plan tier via metadata/lookup_key/price amount, updates user plan; (3) customer.subscription.deleted — downgrades user to free plan; (4) invoice.payment_failed — logs event with user context for debugging. Returns 200 for all handled events (including handler errors to prevent retries), 400 for signature verification failures. All events logged with [Webhook] prefix.
- Timestamp: 2026-02-16
---

## US-042: Subscription management page (billing)
- Status: COMPLETE
- Files changed: src/pages/billing.tsx, services/edge-functions/src/routes/payments.ts, scripts/ralph/prd.json, prd.json
- Notes: Fully rebuilt billing.tsx with plan comparison grid showing all 4 plans (Free, Starter $29/mo, Professional $99/mo, Enterprise custom). Current plan card shows feature list, usage progress bar (grades used / limit), and grade reset date with CalendarClock icon. Current plan highlighted with border-brand-navy border. Upgrade buttons call POST /api/payments/subscribe to create Stripe Checkout subscription sessions. Manage Subscription button calls POST /api/payments/portal to open Stripe Customer Portal for existing subscribers with stripe_customer_id. Enterprise plan directs to sales email. Added two new edge function endpoints: /subscribe (creates subscription checkout with existing customer or customer_email) and /portal (creates billing portal session). Downgrade handled via portal. All loading states with Loader2 spinner, errors via sonner toast.
- Timestamp: 2026-02-16
---

## US-043: Grade certificate public page
- Status: COMPLETE
- Files changed: src/pages/certificate.tsx, package.json, package-lock.json, scripts/ralph/prd.json, prd.json
- Notes: Fully rebuilt certificate.tsx as a public-facing grade certificate page. Fetches grade report by certificate_id via Supabase (public RLS). Shows GradeThread branding header with logo_white.svg and Shield icon. Displays overall score in a large circle with tier badge and color coding. Shows garment photos in a responsive grid gallery with signed URLs. Factor breakdown with 5 horizontal Progress bars matching submission-detail patterns. AI summary section. Footer with graded date (Calendar icon), model version (Cpu icon), certificate ID. QR code via qrcode.react (QRCodeSVG) linking back to the certificate URL in brand navy color. OG meta tags set dynamically via useEffect. Loading skeleton and "Certificate not found" error state. Mobile-responsive layout throughout.
- Timestamp: 2026-02-16
---

## US-044: Dashboard overview: live data integration
- Status: COMPLETE
- Files changed: src/pages/dashboard.tsx, prd.json, scripts/ralph/prd.json
- Notes: Replaced placeholder dashboard with live data integration. Fetches real submission count and last 5 recent submissions from Supabase via TanStack Query (5-min stale time). Recent submissions show title, date, status badge (color-coded), and grade score for completed items. Clicking a submission navigates to /dashboard/submissions/:id. Added "New Submission" CTA button in header linking to /dashboard/submissions/new. "View All" button links to submissions list. Empty state with CTA when no submissions exist. Total Submissions card shows live count with loading skeleton. Grades Used card already uses live profile data (grades_used_this_month). Grade reports fetched for completed submissions and merged. Type assertions used for Supabase query results per tsc -b workaround.
- Timestamp: 2026-02-16
---

## US-045: Grade analytics charts on dashboard
- Status: COMPLETE
- Files changed: src/components/dashboard/grade-charts.tsx (new), src/pages/dashboard.tsx, package.json, package-lock.json, prd.json, scripts/ralph/prd.json
- Notes: Installed recharts library. Created GradeCharts component with 3 charts: (1) Grade Distribution bar chart showing count of grades in each tier bucket (NWT through Poor) with color-coded bars, (2) Average Grade Over Time line chart with 4 weekly data points over last 30 days using brand navy color, (3) Submissions by Garment Type donut pie chart with labels showing percentages. All charts use ResponsiveContainer for responsive sizing. Empty state shows centered message when no completed submissions exist. Loading state shows skeleton cards. Integrated into dashboard.tsx between stats cards and recent submissions. Fixed tsc -b strict type issues: Record indexing returns possibly-undefined values, recharts Tooltip formatter type incompatibility, and Pie label percent possibly undefined.
- Timestamp: 2026-02-16
---

## US-046: Export submissions as CSV
- Status: COMPLETE
- Files changed: src/pages/submissions.tsx, prd.json, scripts/ralph/prd.json
- Notes: Added Export CSV button (with Download icon) to submissions page header alongside New Submission button. Export function fetches all user submissions and their grade reports from Supabase (bypassing pagination), then builds a CSV with 14 columns: Submission Date, Title, Brand, Garment Type, Category, Status, Overall Grade, Grade Tier, Fabric Condition, Structural Integrity, Cosmetic Appearance, Functional Elements, Odor & Cleanliness, Certificate URL. CSV fields properly escaped for commas/quotes/newlines. File downloads as gradethread_export_{YYYY-MM-DD}.csv via Blob URL. Shows loading state on button during export. Empty submissions show info toast. Fixed tsc -b type issue by using .slice(0,10) instead of .split('T')[0] and explicit string[] array typing.
- Timestamp: 2026-02-16
---

## US-047: Settings page: profile editing
- Status: COMPLETE
- Files changed: src/pages/settings.tsx, src/hooks/use-auth.ts, prd.json, scripts/ralph/prd.json
- Notes: Rewrote settings page with full profile editing: editable full name input, avatar upload with FileReader preview and Supabase storage upload, read-only email display, save button that updates users table via .update(). Password change section for email/password users (hidden for OAuth) with current password verification via re-auth, new password + confirm fields, min 6 chars validation. Added refreshProfile() to useAuth hook to reload profile after save. Used 'as never' cast on .update() to work around tsc -b type resolution issue. All toasts via sonner.
- Timestamp: 2026-02-16
---

## US-048: Inventory management: items list page
- Status: COMPLETE
- Files changed: src/pages/inventory.tsx, src/routes/index.tsx, src/components/dashboard/sidebar.tsx, src/lib/constants.ts, prd.json, scripts/ralph/prd.json
- Notes: Created inventory list page with table displaying title, brand, status badge, grade, acquired price, listed price, and days in inventory columns. Added filters for status, garment type, and brand (brand filter dynamically populated from existing items). Sort by date, price, or status. 25-item pagination. Add Item button navigates to /dashboard/inventory/new. Row click navigates to /dashboard/inventory/:id. Added ITEM_STATUSES constant to constants.ts. Added Package icon sidebar nav link. Uses TanStack Query with 5-min stale time. Used explicit cast pattern for Supabase query results (consistent with tsc -b workaround).
- Timestamp: 2026-02-16
---

## US-049: Inventory management: add item form
- Status: COMPLETE
- Files changed: src/pages/inventory-add.tsx, src/routes/index.tsx, prd.json, scripts/ralph/prd.json
- Notes: Created inventory add form page at /dashboard/inventory/new with garment details (title, brand, garment type/category, size, color, condition notes) and acquisition details (price with $ prefix, date defaulting to today, source dropdown). Title and acquired price are required fields. Creates inventory_items record with status 'acquired' via Supabase insert. Two submit options: "Add to Inventory" navigates to item detail page, "Save & Submit for Grading" navigates to new submission page. Used InventoryItemInsert type with 'as never' cast for tsc -b compatibility.
- Timestamp: 2026-02-16
---

## US-050: Inventory item detail page with lifecycle timeline
- Status: COMPLETE
- Files changed: src/pages/inventory-detail.tsx (new), src/routes/index.tsx, prd.json, scripts/ralph/prd.json
- Notes: Created inventory detail page at /dashboard/inventory/:id. Features: header with title, brand, garment type/category and status badge. Lifecycle timeline component with 7 steps (acquired → grading → graded → listed → sold → shipped → completed) shown as connected circles with icons, current step highlighted with ring. Special "returned" state shown separately. Acquisition card with price, date, source, size, color, condition notes. Grading card with link to grade report if graded or Submit for Grading button. Listings section showing all listings with platform, price, date, active/inactive badge, external link. Sales section with sale price, platform fees, date, buyer. Shipments section with carrier, tracking, cost, dates. Profit/loss summary calculating net profit = sale revenue - acquired price - platform fees - shipping costs (green/red color coding). Edit status via dropdown select with all 8 statuses. Used 'as never' cast on .update() for tsc -b compatibility.
- Timestamp: 2026-02-16
---

## US-051: Inventory: add listing to item
- Status: COMPLETE
- Files changed: src/pages/inventory-detail.tsx, src/lib/constants.ts, prd.json, scripts/ralph/prd.json
- Notes: Added Add Listing dialog to inventory detail page with platform dropdown (8 platforms), listing price input, listing URL input (optional), and platform listing ID input (optional). Dialog creates listing record via Supabase insert and auto-updates item status to 'listed' when current status is 'graded' or 'acquired'. Added active/inactive toggle button on each listing row. Added LISTING_PLATFORMS constant to constants.ts and formatPlatform helper with proper display names (e.g. "eBay", "Facebook Marketplace"). Used 'as never' casts on Supabase .insert()/.update() for tsc -b compatibility. Multiple listings per item supported (cross-listing).
- Timestamp: 2026-02-16
---

## US-052: Inventory: record sale
- Status: COMPLETE
- Files changed: src/pages/inventory-detail.tsx, prd.json, scripts/ralph/prd.json
- Notes: Added Record Sale dialog to the Sales card on inventory detail page. Dialog includes: linked listing dropdown (shows active listings with platform name and price, or "No linked listing"), sale price input (required), platform fees input (default 0), sale date input (default today), buyer username input (optional). On submit: creates sale record in Supabase linked to inventory item and optional listing, marks ALL active listings for this item as inactive (cross-listing cleanup), updates item status to 'sold'. Used 'as never' cast on Supabase insert/update for tsc -b compatibility.
- Timestamp: 2026-02-16
---

## US-053: Inventory: record shipment
- Status: COMPLETE
- Files changed: src/pages/inventory-detail.tsx, prd.json, scripts/ralph/prd.json
- Notes: Added Record Shipment dialog to the Shipments card on inventory detail page. Dialog visible when item status is sold or shipped and sales exist. Features: carrier dropdown (USPS, UPS, FedEx, DHL, Other), tracking number input (optional), shipping cost input (required), label cost input (default 0 for pre-paid labels), ship date (default today), weight in oz (optional). Auto-selects sale when only one exists, shows sale selector for multiple sales. Creates shipment record linked to sale, updates item status to shipped. Mark as Delivered button on undelivered shipments sets delivery_date to today and updates item status to completed. Used 'as ShipmentRow' cast for tsc -b compatibility on spread operations.
- Timestamp: 2026-02-16
---

## US-054: Financial dashboard overview
- Status: COMPLETE
- Files changed: src/pages/finances.tsx (new), src/routes/index.tsx, src/components/dashboard/sidebar.tsx, prd.json, scripts/ralph/prd.json
- Notes: Created financial dashboard page at /dashboard/finances. Features 8 metric cards in two rows: Row 1 - Total Revenue, Total Costs, Net Profit, Profit Margin %. Row 2 - Items Sold, Avg Profit/Item, Avg Days to Sell, Inventory Value. Period toggle with 5 options (This Month, Last 30 Days, This Quarter, This Year, All Time) filters sales by date. Revenue = sum of sale prices, costs = acquisition + shipping + label + platform fees, inventory value = sum of acquired_price for unsold items (acquired/grading/graded/listed statuses). All data fetched via Supabase queries with loading skeletons. Color coding: green for revenue/positive profit, red for costs/losses. Used explicit type casts for tsc -b compatibility.
- Timestamp: 2026-02-16
---

## US-055: Financial reporting: profit/loss per item
- Status: COMPLETE
- Files changed: src/components/finances/profit-table.tsx (new), src/pages/finances.tsx, prd.json, scripts/ralph/prd.json
- Notes: Created ProfitTable component with all 10 required columns (Item Title, Brand, Acquired Price, Grading Cost, Listed Price, Sale Price, Platform Fees, Shipping Cost, Net Profit, Profit Margin %). All columns sortable via header buttons with directional arrows. Filters: date range (from/to date inputs), profit/loss toggle (Select dropdown), brand filter, category filter. Green/red color coding on Net Profit and Margin columns. Totals footer row sums all monetary columns and calculates aggregate margin. Added listings fetch to finances page data query for Listed Price column. Grading Cost column present as placeholder (0) since no grading fee field in DB yet.
- Timestamp: 2026-02-16
---

## US-056: Financial reporting: revenue and profit charts
- Status: COMPLETE
- Files changed: src/components/finances/financial-charts.tsx (new), src/pages/finances.tsx, prd.json, scripts/ralph/prd.json
- Notes: Created FinancialCharts component with 5 charts using recharts: (1) Revenue & Profit over time line chart with daily/weekly/monthly granularity toggle, (2) Cost breakdown pie chart (acquisition, shipping, platform fees, grading fees), (3) Top 5 brands by profit horizontal bar chart, (4) Top 5 categories by profit vertical bar chart. All charts respond to the period filter from the finances page. Used recharts Tooltip formatter pattern from grade-charts.tsx (Number(value) cast) to avoid tsc -b type errors.
- Timestamp: 2026-02-16
---

## US-057: Financial reporting: cash flow timeline
- Status: COMPLETE
- Files changed: src/components/finances/cash-flow.tsx (new), src/pages/finances.tsx, prd.json, scripts/ralph/prd.json
- Notes: Created CashFlow component with dual-view toggle (chart/table). Chart view: bar chart showing inflows (green) and outflows (red) by date, plus line chart showing cumulative cash position with running balance. Table view: chronological transaction list with date, type (In/Out with icons), category, description, amount, and running balance columns. Transaction types include item acquisitions (outflow), sales revenue (inflow), platform fees (outflow), and shipping costs (outflow). Hover tooltips show formatted currency values. Used same recharts Tooltip formatter pattern (Number(value)) as financial-charts.tsx.
- Timestamp: 2026-02-16
---

## US-058: Financial reporting: tax-ready export
- Status: COMPLETE
- Files changed: src/components/finances/financial-export.tsx (new), src/pages/finances.tsx, prd.json, scripts/ralph/prd.json
- Notes: Created FinancialExport component with date range selector (defaults to current tax year), dropdown menu with CSV and PDF export options. CSV export generates file with summary section (gross revenue, expenses by category, net profit) followed by transaction details (date, type, category, amount, item title, platform). PDF export opens a styled HTML document in a new window for browser print-to-PDF. Transactions are built from inventory items (acquisition expenses), sales (income + platform fees), and shipments (shipping costs). File naming follows gradethread_financial_report_{startDate}_{endDate}.csv convention. Used existing CSV escape pattern from submissions.tsx.
- Timestamp: 2026-02-16
---

## US-059: Analytics: time-on-market tracking
- Status: COMPLETE
- Files changed: src/pages/inventory.tsx, src/components/finances/time-on-market.tsx (new), src/pages/finances.tsx, prd.json, scripts/ralph/prd.json
- Notes: Updated inventory page to show "Days Listed" column calculated from earliest listing date to sale date (for sold items) or today (for active items), replacing the old "Days in Inventory" column. Added yellow highlighting for 30+ days and red for 60+ days. Created TimeOnMarket analytics component with: overall average days-to-sell metric, slowest category summary card, slow-moving items count card, time-to-sell distribution histogram (6 buckets), average days by garment type table, average days by brand table, and slow-moving inventory section showing unsold items listed 30+ days. Fetches listings and sales data in inventory page for the days-listed calculation. Used non-null assertion for array[0] access after .length check to satisfy tsc -b strict mode.
- Timestamp: 2026-02-16
---

## US-060: Analytics: price suggestion engine
- Status: COMPLETE
- Files changed: src/lib/price-suggestions.ts (new), src/pages/price-suggestions.tsx (new), src/pages/inventory-detail.tsx, src/routes/index.tsx, src/components/dashboard/sidebar.tsx, prd.json, scripts/ralph/prd.json
- Notes: Created price suggestion algorithm in src/lib/price-suggestions.ts with calculateSuggestedPrice function. Algorithm uses grade tier multipliers (NWT 1.3x down to Poor 0.6x), brand/category matching for comparable sales, grade-weighted averaging when sufficient data exists, and time-based reduction thresholds (14d: -7%, 30d: -15%, 60d: -25% with auction suggestion). Added callout card on inventory detail page with severity-colored left border (blue/yellow/red), showing current vs suggested price and adjustment percentage. Created full suggestions page at /dashboard/analytics/suggestions with summary cards (total items, urgent actions, warnings) and detailed table with all items sorted by severity. Added sidebar navigation item with Lightbulb icon.
- Timestamp: 2026-02-16
---

## US-061: Analytics: listing optimization suggestions
- Status: COMPLETE
- Files changed: src/components/analytics/listing-suggestions.tsx (new), src/pages/dashboard.tsx, src/pages/inventory-detail.tsx, prd.json, scripts/ralph/prd.json
- Notes: Created listing-suggestions.tsx with generateListingSuggestions engine producing three suggestion types: (1) add_platforms when item listed on only 1 platform, (2) price_adjustment when listed 14+ days without selling (severity escalates at 30d/60d), (3) regrade_photos when grade confidence < 0.75. Two display components exported: ListingSuggestions (card with list, used on dashboard with maxItems=5) and InventoryItemSuggestions (individual cards, used on inventory detail page). Dismissible suggestions stored in localStorage under gradethread_dismissed_suggestions key. Dashboard uses separate TanStack Query for inventory/listings/grade_reports data. tsc -b caught array index access issue requiring guard for activeListings[0].
- Timestamp: 2026-02-16
---

## US-062: Dispute submission form and tracking
- Status: COMPLETE
- Files changed: src/pages/submission-detail.tsx, src/pages/submissions.tsx, prd.json, scripts/ralph/prd.json
- Notes: Added Dispute Grade button on submission detail page (visible for completed grades within 7 days, hidden if dispute already exists). Dispute dialog with reason textarea (min 20 chars, character counter) and optional multi-photo upload for additional evidence. Creates dispute record linked to grade_report and user_id, updates submission status to 'disputed'. Dispute status card displayed on submission detail page with color-coded badge (open=yellow, under_review=blue, resolved=green, rejected=red), reason, resolution notes, and pending review message. My Disputes section added to submissions page with table showing submission title, status, reason, filed date, and resolution notes — clicking navigates to the submission detail. Used 'as GradeReportRow' cast for tsc -b compatibility. No new lint errors.
- Timestamp: 2026-02-18
---

## US-063: API key management page
- Status: COMPLETE
- Files changed: src/pages/api-keys.tsx, services/edge-functions/src/routes/api-keys.ts (new), services/edge-functions/src/main.ts, prd.json, scripts/ralph/prd.json
- Notes: Created edge function routes for API key CRUD at /api/keys with auth middleware. POST creates key with crypto.getRandomValues (32 bytes), SHA-256 hashing for storage, returns full key once. GET lists user's keys (id, name, prefix, dates). DELETE revokes by id with ownership check. Server-side plan gating (Professional/Enterprise only) and 10-key limit. Frontend page rebuilt with plan gate (Crown upgrade prompt for Free/Starter), TanStack Query for key list, create dialog with name + optional expiry date, one-time full key display with copy-to-clipboard and security warning, revoke confirmation dialog. Key format: gt_sk_{64 hex chars}, prefix shows first 14 chars. Fixed pre-existing tsc -b stale cache issue (recharts/qrcode.react types were resolvable but tsBuildInfo was stale).
- Timestamp: 2026-02-18
---

## US-064: Edge function: API key generation and validation
- Status: COMPLETE
- Files changed: services/edge-functions/src/middleware/api-key-auth.ts (new), services/edge-functions/src/main.ts, prd.json, scripts/ralph/prd.json
- Notes: Created api-key-auth.ts middleware that validates API keys from the X-API-Key header. Hashes the provided key with SHA-256 and matches against stored key_hash in api_keys table. Validates key format (gt_sk_ prefix + 64 hex chars = 70 chars total). Checks if key is expired via expires_at timestamp. Updates last_used_at as fire-and-forget (non-blocking) on successful validation. Sets user and userId on Hono context from the key's user_id, matching the same interface as the JWT auth middleware. Updated CORS configuration to allow X-API-Key header. POST/DELETE routes and key generation logic already existed from US-063.
- Timestamp: 2026-02-18
---

## US-065: Public REST API: grade submission and retrieval
- Status: COMPLETE
- Files changed: services/edge-functions/src/routes/api-v1.ts (new), services/edge-functions/src/main.ts, prd.json, scripts/ralph/prd.json
- Notes: Created api-v1.ts route file with 3 endpoints versioned under /api/v1/. POST /api/v1/grades accepts JSON body with garment info and images array (each image has image_type + url or base64), validates required fields and image types, downloads/decodes images, uploads to Supabase Storage, creates submission, triggers grading pipeline, returns 202 with { data: { id, status }, error: null, meta: null }. GET /api/v1/grades/:id returns full submission info with grade report (all factor scores, AI summary, certificate_id) for completed grades. GET /api/v1/grades returns paginated list with ?page=&limit=&status= query params, includes grade summary for completed items, returns meta with page/limit/total/total_pages/has_next/has_prev. All responses follow consistent { data, error, meta } JSON structure. Authenticated via X-API-Key header using apiKeyAuthMiddleware. Rate limited at 100 req/min via existing rateLimiter middleware. Plan limit checking and usage increment reuse same logic as internal grade routes.
- Timestamp: 2026-02-18
---

## US-066: Webhook delivery system for grade completion
- Status: COMPLETE
- Files changed: supabase/migrations/00005_api_keys_webhook_url.sql (new), services/edge-functions/src/lib/webhook-delivery.ts (new), services/edge-functions/src/routes/api-v1.ts, services/edge-functions/src/lib/grading-pipeline.ts, services/edge-functions/src/main.ts, src/types/database.ts, prd.json, scripts/ralph/prd.json
- Notes: Migration 00005 adds nullable webhook_url column to api_keys table and an UPDATE RLS policy. Created webhook-delivery.ts with notifyWebhooks() orchestrator, HMAC-SHA256 signature computation using key_hash as secret, retry logic with exponential backoff (5s, 30s, 120s = 3 retries after initial attempt), 10s delivery timeout, and comprehensive logging of all attempts/responses. PATCH /api/v1/webhook endpoint validates URL format (http/https), allows null to clear, and updates all user's API keys. Grading pipeline triggers notifyWebhooks as fire-and-forget after grade completion (Step 8). Added PATCH to CORS allowMethods. Updated ApiKeyRow and ApiKeyInsert types with webhook_url field. Typecheck and build pass clean.
- Timestamp: 2026-02-18
---

## US-067: Admin layout and role-based access control
- Status: COMPLETE
- Files changed: src/components/auth/admin-route.tsx (new), src/layouts/admin-layout.tsx (new), src/pages/admin/dashboard.tsx (new), src/pages/admin/users.tsx (new), src/pages/admin/submissions.tsx (new), src/pages/admin/reviews.tsx (new), src/pages/admin/ai-models.tsx (new), src/pages/admin/system.tsx (new), src/routes/index.tsx, src/components/dashboard/header.tsx, prd.json, scripts/ralph/prd.json
- Notes: AdminRoute guard component checks profile.role for 'admin' or 'super_admin', redirects non-admins to /dashboard with 'Access denied' toast (using useRef to prevent duplicate toasts), redirects unauthenticated to /login. AdminLayout uses brand-night (#1A1A2E) background for darker treatment distinct from dashboard's brand-navy sidebar. Active nav items use brand-red highlight instead of white. Admin header shows "Admin Panel" badge. "Back to Dashboard" link at sidebar bottom. Six placeholder admin pages created under src/pages/admin/. Routes wrapped in AdminRoute guard at /admin/*. Shield icon admin link added to dashboard header dropdown, only rendered when profile.role is admin or super_admin.
- Timestamp: 2026-02-18
---

## US-068: Admin dashboard with KPI overview
- Status: COMPLETE
- Files changed: src/pages/admin/dashboard.tsx, supabase/migrations/00006_admin_read_policies.sql (new), prd.json, scripts/ralph/prd.json
- Notes: Replaced placeholder admin dashboard with full implementation. 8 KPI cards in 2 rows: Primary row — Total Users, Active Subscribers, Submissions Today, Revenue This Month. Secondary row — Average Grade, Dispute Rate %, AI Accuracy % (high-confidence rate), Pending Reviews count. 3 line charts (recharts): Submission Volume (daily, 30 days, brand navy), Revenue (daily, 30 days, green), New Users (daily, 30 days, brand red). All data fetched from Supabase in parallel via Promise.all with admin RLS policies. Auto-refreshes every 60 seconds via TanStack Query refetchInterval. Created migration 00006 adding admin SELECT policies on users, submissions, grade_reports, disputes, and sales tables so admin users can query platform-wide data. Used same recharts patterns (Tooltip Number() cast, TOOLTIP_STYLE constant) as existing charts for consistency. Typecheck and build pass clean.
- Timestamp: 2026-02-18
---

## US-069: Admin: user management list and detail
- Status: COMPLETE
- Files changed: src/pages/admin/users.tsx, src/pages/admin/user-detail.tsx (new), src/routes/index.tsx, src/components/ui/alert-dialog.tsx (shadcn), prd.json, scripts/ralph/prd.json
- Notes: Rebuilt users.tsx from placeholder to full admin user management list. Table with 7 columns (name, email, plan, role, grades used, signup date, last active). Search by name or email, filter by plan/role/signup date range. Paginated at 20 rows. Click row navigates to /admin/users/:id. Created user-detail.tsx with profile card (avatar, name, email, plan, role, join date, Stripe ID), usage stats (4 metric cards), admin actions section with 3 controls (change plan dropdown, change role dropdown, suspend/unsuspend button), subscription info card, and submission history table (last 20 with status badges and grade scores). All admin actions create admin_audit_log entries with details object. AlertDialog confirmation for all destructive actions. Added shadcn alert-dialog component. Added route /admin/users/:id.
- Timestamp: 2026-02-18
---

## US-070: Admin: submission queue and monitoring
- Status: COMPLETE
- Files changed: src/pages/admin/submissions.tsx, prd.json, scripts/ralph/prd.json
- Notes: Rebuilt placeholder admin submissions page with full implementation. Table with 8 columns (title, user email, status badge, grade score, confidence %, date, processing time, actions). Search by title/email/name. Filters for status (5 options), confidence level (high/medium/low), and date range. Sortable by date, confidence, and processing time with toggle direction. 25-row pagination. Click row navigates to submission detail. Three admin actions per row: (1) View AI Raw Analysis dialog showing full grade report with scores, factor breakdown, AI summary, detailed notes JSON, and metadata; (2) Re-trigger Grading with AlertDialog confirmation that resets status to 'processing'; (3) Mark as Failed for stuck processing items. Dedicated Failed Submissions section with table showing failed items and re-trigger buttons. 5-minute SLA indicator: overdue processing items highlighted with red background, clock icon in status badge, red processing time text, and warning banner showing count of overdue items. All admin actions create audit log entries.
- Timestamp: 2026-02-18
---

## US-071: Admin: human review queue
- Status: COMPLETE
- Files changed: src/pages/admin/reviews.tsx, prd.json, scripts/ralph/prd.json, scripts/ralph/progress.txt
- Notes: Replaced placeholder with full human review queue implementation. Queue table shows submission title, garment type, AI overall score, confidence %, grade tier badge, and waiting time (with Clock icon). Filters: unreviewed-only vs all, confidence range (high/medium/low), and text search by title/email/name. Sort by waiting time (oldest first default) or confidence (lowest first). Review detail dialog shows: submitted photos grid with signed URLs from Supabase Storage (loading skeleton while fetching), AI grade and confidence cards, AI summary text, and editable factor scores (5 inputs with weight labels, AI original values shown, computed weighted overall with live delta display). Three reviewer actions: (1) Approve As-Is creates human_reviews record with original score unchanged, (2) Adjust & Approve creates human_reviews record with adjusted_score AND updates grade_report factor scores + overall, (3) Reject & Re-grade creates human_reviews record and resets submission to processing status. Score adjustments >1.5 points show amber warning banner; non-super_admin users have Adjust button disabled. Already-reviewed items show green info banner with previous review details. All actions logged to admin_audit_log with action/target/details. Stats badges in header show pending/reviewed/total counts. Pagination at 20 items per page. Typecheck and build pass clean.
- Timestamp: 2026-02-18
---

## US-072: Admin: dispute management
- Status: COMPLETE
- Files changed: src/pages/admin/disputes.tsx, src/routes/index.tsx, src/layouts/admin-layout.tsx, prd.json, scripts/ralph/prd.json, scripts/ralph/progress.txt
- Notes: Created full admin dispute management page at /admin/disputes. Dispute queue table with user (name + email), submission title, dispute reason, status badge, created date, and waiting time. Filter by status (open/under_review/resolved/rejected) and text search across title/email/name/reason. Sort by date or waiting time. Detail dialog shows: dispute status banner with timing, user's dispute reason in amber highlighted card, original grade report (overall score + confidence), AI summary, submission photos grid with signed URLs. Admin actions: (1) Mark as Under Review updates dispute status, (2) Resolve with optional grade adjustment — toggle checkbox enables factor score editor (same weighted scoring as reviews page), creates human_review record when adjusting, updates grade_report scores, sets dispute to resolved with notes, sets submission status to completed, (3) Reject with required reason — confirmation dialog with textarea, sets dispute to rejected, sets submission to completed. Already resolved/rejected disputes show read-only status banner with resolution notes. Stats cards show counts for each status. All actions logged to admin_audit_log. Added Scale icon nav item to admin sidebar. Typecheck and build pass clean.
- Timestamp: 2026-02-18
---

## US-073: Admin: AI model management and prompt versioning
- Status: COMPLETE
- Files changed: src/pages/admin/ai-models.tsx, prd.json, scripts/ralph/prd.json, scripts/ralph/progress.txt
- Notes: Replaced stub with full AI model management page at /admin/ai-models. Route and sidebar nav were already configured from prior stub creation. Features implemented: (1) KPI cards — active prompt name, average accuracy across versions, total grades processed, version count. (2) Accuracy trend chart — horizontal bar chart showing accuracy % per version with grade count, sorted chronologically. (3) Versions table — sortable columns (name, accuracy, grades, created date), filterable by status (active/inactive) and text search, paginated at 15 rows. Row actions: view, test prompt, activate/deactivate, delete. (4) Create dialog — version name input + prompt text textarea with edit/preview tabs for markdown rendering, character count. Created as inactive by default. (5) View/edit dialog — stats cards (status, accuracy, grades), inline prompt text viewer with Edit toggle, edit mode with same tabs interface. Quick actions bar for test/activate/delete. (6) Compare dialog — select two versions from dropdowns, side-by-side metrics cards plus side-by-side prompt text panels with character counts. (7) Activate confirmation — deactivates all other versions, activates selected (only one active at a time). (8) Deactivate confirmation with warning about no active prompt. (9) Delete with safety check — cannot delete active version, shows grade count warning. (10) Test prompt dry-run dialog with loading spinner and structured result output. (11) Audit logging for all mutations via admin_audit_log table. Follows existing admin page patterns. Typecheck and build pass clean.
- Timestamp: 2026-02-18
---

## US-074: Admin: system health and traffic monitoring
- Status: COMPLETE
- Files changed: src/pages/admin/system.tsx, prd.json, scripts/ralph/prd.json, scripts/ralph/progress.txt
- Notes: Replaced stub with full system health monitoring page at /admin/system. Route and sidebar nav were already configured from prior stub creation. Features: (1) Service health indicators — DB latency-based status (healthy <500ms, degraded <2s, down), storage availability, API status, color-coded cards with status icons. (2) Queue metrics — 4 cards showing pending count, processing count, average processing time (minutes), and failed submissions in last 24h. (3) Storage usage — total images count and estimated storage size with progress bar. (4) Traffic charts — hourly submissions bar chart (last 24h) with unique users overlay, daily unique users line chart (last 30d). (5) Error rate — daily failure rate bar chart (last 7d) with summary totals. (6) Subscription metrics — plan distribution pie chart (4 plans, color-coded), MRR, paid users, churn rate estimate, ARPU cards. (7) Auto-refresh every 30 seconds via TanStack Query refetchInterval. Manual refresh button with loading spinner. Last refresh timestamp display. All data fetched via Supabase admin RLS queries. Used existing recharts patterns (Tooltip Number() cast, TOOLTIP_STYLE constant). Typecheck and build pass clean.
- Timestamp: 2026-02-18
---

## US-075: Admin: AI accuracy feedback loop
- Status: COMPLETE
- Files changed: services/edge-functions/src/lib/accuracy-tracking.ts (new), src/pages/admin/ai-models.tsx, prd.json, scripts/ralph/prd.json, scripts/ralph/progress.txt
- Notes: Created accuracy-tracking.ts edge function module with: calculateReviewAccuracy (per-factor |AI_score - human_score|), computeAccuracySummary (aggregate MAE, agreement rate within 0.5 points, Pearson correlation per prompt version), updatePromptVersionAccuracy (fire-and-forget after reviews), exportTrainingDataset (JSONL), computeWeeklyAccuracySummary (last 7 days). Enhanced AI Models page with: (1) Accuracy Feedback Dashboard section using recharts ComposedChart — agreement rate bars + MAE line + threshold reference line per version, detail tab with per-factor breakdown using Progress bars. (2) Export JSONL button downloads training dataset with AI scores + human adjustments. (3) Weekly Summary button computes 7-day accuracy on-demand with toast. (4) Red alert banner when version drops below 80% agreement threshold. All accuracy computed from real human_reviews + grade_reports joined by grade_report_id -> model_version. Fixed tsc -b strict issues: non-null assertions for array indexing in pearson correlation, Record index access guards, recharts Tooltip formatter types with unknown casts, useMemo wrapping for logical expressions. Typecheck, build, and lint (new files only) pass clean.
- Timestamp: 2026-02-18
---

## US-076: Email notification system
- Status: COMPLETE
- Files changed: services/edge-functions/src/lib/email.ts (new), services/edge-functions/src/routes/notifications.ts (new), services/edge-functions/src/lib/grading-pipeline.ts, services/edge-functions/src/main.ts, services/edge-functions/.env.example, src/pages/admin/disputes.tsx, src/pages/signup.tsx, prd.json, scripts/ralph/prd.json
- Notes: Created email.ts utility using Resend API with branded HTML email templates. Three template functions: sendGradeCompleteEmail (large score card with color-coded tier, report link, certificate share link), sendDisputeResolvedEmail (outcome badge, grade adjustment display, reviewer notes), sendWelcomeEmail (3-step getting started guide with numbered steps). All emails use GradeThread branding: navy header with logo text, white content area, night-colored footer. Red CTA buttons. Gracefully skips if RESEND_API_KEY not configured. Grade complete email fires as fire-and-forget in grading-pipeline.ts (Step 9) after grade report creation — fetches user email/name from DB before sending. Dispute resolved/rejected email triggered via new POST /api/notifications/dispute-resolved edge function endpoint (auth-required), called from admin disputes page after resolve/reject actions. Welcome email triggered via POST /api/notifications/welcome (unauthenticated, IP-rate-limited at 5/min), called from signup page after successful signUpWithEmail. Notifications route registered in main.ts. Welcome email includes unsubscribe link to /dashboard/settings. HTML escaping applied to all user-provided content.
- Timestamp: 2026-02-18
---

## US-137: FlipDesk end-to-end integration test + dogfood pass
- Status: COMPLETE (automated harness) + operator-gated remainder documented
- Files changed: e2e/flipdesk-lifecycle.spec.ts (new), src/lib/__tests__/flipdesk-lifecycle.test.ts (new), FLIPDESK_DOGFOOD.md (new), prd.json
- Notes: Built the automated half of the lifecycle gate. Playwright spec drives the
  money loop UI (login -> pipeline board across sourced..sold..completed -> sold P&L)
  on the SAME page.route mock seam as critical-path.spec.ts -- no eBay sandbox/staging
  creds, no AI spend. Deterministic Vitest suite (10 tests) pins status-transition
  rules (validateStatusChange + FLIPDESK_PIPELINE order), fee allocation, and net_profit
  P&L, proving computePnl agrees to the cent with the edge sync net_profit formula
  (flipdesk-ebay.ts, incl. US-459 refund-reversal). Verified: tsc -b clean, eslint clean,
  vitest 10/10, full e2e suite 13/13 green, new spec 3x repeat-each stable.
  GOTCHA: register the `**/api/**` catch-all BEFORE the specific `**/api/payments/**`
  billing route -- Playwright matches the most-recently-added route first, so a trailing
  catch-all silently shadows billing-summary and the dashboard header crashes the page
  into its error boundary. AC#2 live-infra run + AC#4 manual dogfood/friction log + AC#5
  4-week 50% items-listed/week metric are operator actions (need eBay sandbox + a real
  garment + 4 weeks of use) -> captured as a ready-to-execute runbook in FLIPDESK_DOGFOOD.md.
- Timestamp: 2026-06-11
---

## US-146: Google Sheets OAuth + connection setup
- Status: COMPLETE
- Files changed:
  - supabase/migrations/00131_google_connections.sql (new — google_connections + google_oauth_states)
  - services/edge-functions/src/routes/flipdesk-google.ts (new — OAuth + sheet create/use + disconnect + token refresh)
  - services/edge-functions/src/main.ts (import, auth/workspace middleware per-path, route mount, rate limiters)
  - services/edge-functions/src/lib/env-validation.ts (google_sheets FEATURE_GROUP)
  - services/edge-functions/src/tests/google-sheets-oauth_test.ts (new — consent scopes + config gating)
  - services/edge-functions/src/tests/rls-guard_test.ts (google_oauth_states -> SERVICE_ROLE_ONLY)
  - services/edge-functions/.env.example (GOOGLE_SHEETS_* docs)
  - src/hooks/use-google-sheets.ts (new — connection/config/oauth/create/use/disconnect hooks)
  - src/pages/flipdesk/marketplaces-google.tsx (new — setup wizard)
  - src/pages/flipdesk/marketplaces.tsx (entry card linking to wizard)
  - src/routes/index.tsx (lazy route /dashboard/flipdesk/marketplaces/google)
  - src/types/database.ts (GoogleConnection Row/Insert/Update + tables map)
- Notes:
  - oauth_states could NOT be reused for Google: its `marketplace` column is the
    listing_platform enum, and Google isn't a listing platform. Added a dedicated
    single-use google_oauth_states table (service-role only, self-expiring).
  - Encryption uses crypto-aes.ts encryptToken/decryptToken (AES-256-GCM, AAD=user_id),
    the canonical helper that superseded US-119's FLIPDESK_INTERNAL_JOB_SECRET approach
    (same one google-photos + eBay already use). The AC's mention of the job-secret key
    is stale.
  - Mounting order matters: /api/flipdesk/google/photos is mounted (and its public
    callback wired) BEFORE /api/flipdesk/google; the new google routes are listed
    per-path in middleware (not a /google/* wildcard) so they never shadow the public
    Photos callback.
  - Scopes: drive.file (per-file, NOT full drive) + spreadsheets, access_type=offline +
    prompt=consent so Google always returns a refresh token (even on reconnect).
  - DEVIATION from AC#4: "Use existing sheet" is a paste-link/id input validated
    server-side via Sheets API (clear 403 when the app lacks drive.file access to that
    file) rather than the full gapi Drive Picker. The Picker needs a client-side Google
    API key that isn't provisioned yet (the story's original blocker). "Create new sync
    sheet" is the fully-wired primary path; the Picker can be layered in later behind a
    VITE_GOOGLE_API_KEY without touching the server contract.
  - "Migration 00017" in the AC was a stale story-era number; next free migration is 00131.
  - First `npm run build` hit a transient esbuild vite:dep-scan error on an UNRELATED file
    (use-remove-bg, which exists and was unmodified); a clean re-run exits 0.
- Timestamp: 2026-06-11
---

## US-147: Google Sheets 2-way live sync (push + pull + conflict resolution)
- Status: COMPLETE
- Files changed: supabase/migrations/00132_google_sheet_sync.sql, services/edge-functions/src/lib/sheet-sync.ts (new), services/edge-functions/src/lib/google-sheets-api.ts (new), services/edge-functions/src/routes/flipdesk-google-sync.ts (new), services/edge-functions/src/routes/flipdesk-google.ts, services/edge-functions/src/main.ts, services/edge-functions/src/lib/schema-version.ts, services/edge-functions/src/tests/rls-guard_test.ts, services/edge-functions/src/tests/google-sheet-sync_test.ts (new), src/hooks/use-google-sheets.ts, src/pages/flipdesk/marketplaces-google.tsx, LAUNCH_CHECKLIST.md
- Notes:
  * Conflict detection needs a 3rd copy of the data: google_sheet_sync_state stores per-field values as of the last sync ("base"). sheet!=base & db==base -> pull; db!=base & sheet==base -> push; both changed -> conflict (FlipDesk wins, row flagged in Conflicts tab with Accept Sheet dropdown applied on next run).
  * KEY DESIGN DECISION: /sync/push and /sync/pull both run the SAME full bidirectional merge. A push-only pass that rewrites whole rows would clobber un-pulled sheet edits; since push needs to read the sheet anyway (row mapping by hidden flipdesk_id), the merge is free.
  * Field allow-list enforced structurally: only ColumnDef.writable columns can ever produce pulls (title, brand, size, sku, status, acquired_price=cost_basis, target_price, condition_notes=notes). Invalid values (bad enum, negative price, blanked title) are reverted in the sheet + logged to Sync Log.
  * Values compared after normalization ("$12.50" in sheet == numeric 12.5 from PostgREST); writes use RAW + reads use UNFORMATTED_VALUE to keep round-trips stable.
  * Two CI guards fired on the new migration (good!): schema-version_test wants EXPECTED_SCHEMA_VERSION bumped in the same commit, and rls-guard_test requires classifying any zero-policy table in SERVICE_ROLE_ONLY.
  * Listings/Sales tenant-scoping goes through inventory_items!inner(user_id) since those tables carry no user_id (US-268 pattern).
  * Docker was down so the local supabase db reset lane was skipped (simple CREATE TABLE migration; CI db-migrations.yml will verify).
- Timestamp: 2026-06-11 09:35
---

## US-148: 3-way reconciliation (FlipDesk <-> eBay <-> Google Sheets) with conflict UI
- Status: COMPLETE
- Files changed: src/types/database.ts, src/hooks/use-sync-conflicts.ts (new), src/components/flipdesk/cross-source-conflicts.tsx (new), src/pages/flipdesk/reconciliation.tsx, services/edge-functions/src/routes/flipdesk-reconciliation.ts, services/edge-functions/src/routes/flipdesk-google-sync.ts, services/edge-functions/src/lib/schema-version.ts, services/edge-functions/src/tests/rls-guard_test.ts
- Notes: Detection layer (lib/sync-conflicts.ts + migration 00133, not 00018 - numbering moved on) pre-existed from commit de37d96; this iteration added the missing surface: edge endpoints GET /conflicts + POST /conflicts/resolve on the reconciliation route (Business-gated, tenant-scoped per US-268), the Cross-source tab (3-col diff, per-field picks, per-listing + multi-select Accept-all-from-X, mark-ended auto-flag CTA, email-threshold card). Key find: flipdesk-google-sync.ts imported recordSourceObservations but never called it (deno lint caught it) - wired Sheets observations into the read-only Listings-tab merge; undecided conflicted cells are HELD from the DB-authoritative rewrite so the divergent sheet value survives until the user picks a winner, and agreeing observations are reported for open-conflict listings so they auto-converge. Resolving picks flipdesk/ebay/sheets per field, applies the value to listings (price/quantity/status+is_active/title), pins listings.source_of_truth (eBay pull respects it via pinnedAgainstEbay), closes the row. Also fixed two guard tests failing since de37d96: EXPECTED_SCHEMA_VERSION 00132->00133 and flipdesk_sync_conflicts added to SERVICE_ROLE_ONLY in rls-guard_test. Verified: tsc -b, eslint, vite build, deno lint/check, 653 edge tests, 402 web tests.
- Timestamp: 2026-06-11 14:30
---

## US-149: Multi-marketplace cross-listing dispatch
- Status: COMPLETE
- Files changed: supabase/migrations/00134_cross_listing_dispatch.sql; services/edge-functions/src/lib/marketplace-adapters/{types,index,ebay,poshmark,mercari,depop}.ts; services/edge-functions/src/lib/cross-listings.ts; services/edge-functions/src/routes/flipdesk-listings.ts; services/edge-functions/src/routes/flipdesk-ebay.ts (auto-end hook in orders sync); services/edge-functions/src/main.ts (mount + auth/workspace/rate-limit); services/edge-functions/src/lib/schema-version.ts (00134 bump); services/edge-functions/src/tests/marketplace-adapters_test.ts; src/types/database.ts; src/lib/constants.ts (CROSS_LISTING_PLATFORMS); src/hooks/use-cross-listing.ts; src/pages/flipdesk/composer.tsx (Push to multi-select + per-platform prices); src/pages/flipdesk/listings.tsx (Platforms column); src/pages/flipdesk/marketplaces.tsx (auto-end toggle)
- Notes: Source draft self-references draft_id so one lookup finds the whole group. eBay path reuses publishItemForOwner; other adapters return structured 501 (rows created locally, publish pending) — test asserts the contract without importing the eBay adapter (it pulls the service-role supabase client). Auto-end is wired into the eBay orders sync (the only live sale-detection path); manual RecordSaleDialog sales do not trigger it yet. Gotcha: adding a migration requires bumping EXPECTED_SCHEMA_VERSION in the same commit (schema-version_test.ts fails otherwise). Frontend supabase writes to new tables need `as never` casts (typed client resolves tables to never). eBay-only selection keeps the existing preflight dialog so single-platform UX is unchanged.
- Timestamp: 2026-06-11 14:55 CT
---

## US-150: Price-drop and promo scheduler
- Status: COMPLETE
- Files changed: supabase/migrations/00135_flipdesk_automation_rules.sql, services/edge-functions/src/lib/automation-rules.ts, services/edge-functions/src/routes/flipdesk-automations.ts, services/edge-functions/src/tests/automation-rules_test.ts, services/edge-functions/src/main.ts, services/edge-functions/src/lib/schema-version.ts, services/edge-functions/COOLIFY.md, src/types/database.ts, src/hooks/use-automations.ts, src/pages/flipdesk/automations.tsx, src/pages/flipdesk/item.tsx, src/routes/index.tsx, src/components/dashboard/sidebar.tsx
- Notes: Migration is 00135, not the AC's stale 00020. Reused US-672 repricing patterns (push-to-eBay-first per US-467, job lock, repricing kill-switch). set_promo_rate_pct is LOCAL-FIRST (listings.promo_rate_pct) because no eBay Marketing API client exists — same deferral precedent as US-141's promote. no_views_in_days implemented as "zero lifetime views and listed > N days" (no per-day view history exists). Added cooldown_days per trigger (default 7) so the hourly cron can't re-apply a markdown every hour. Margin floor uses integer-cents math (cost*1.1 in floats = 1650.0000000000002 — caught by unit test). Gotcha: schema-version_test.ts requires bumping EXPECTED_SCHEMA_VERSION in lib/schema-version.ts in the same commit as any new migration. Scope JSON reuses the US-143 FilterQuery vocabulary so the web FilterBuilder is reused verbatim; server stores rules without ids, UI re-hydrates them.
- Timestamp: 2026-06-11 15:45 (local)
---

## US-151: Listing-performance sync: views, watchers, impressions per listing
- Status: COMPLETE
- Files changed: supabase/migrations/00136_listing_performance_metrics.sql (new), services/edge-functions/src/lib/ebay-client.ts, services/edge-functions/src/routes/flipdesk-ebay.ts, services/edge-functions/src/tests/tenant-isolation_test.ts, services/edge-functions/COOLIFY.md, LAUNCH_CHECKLIST.md, src/types/database.ts, src/hooks/use-ebay.ts, src/components/dashboard/sidebar.tsx, src/routes/index.tsx, src/pages/flipdesk/listing-performance.tsx (new), src/pages/flipdesk/overview.tsx, src/pages/flipdesk/listings.tsx
- Notes: Sell Analytics getTrafficReport pulled per active eBay connection by job-auth POST /api/flipdesk/ebay/sync/performance (6h cron). New listings metric columns + rolling view_trend_7d for the sparkline; marketplace_connections.analytics_access_denied flag drives graceful degradation (reconnect banner+toast). Added sell.analytics.readonly scope (pre-scope tokens degrade until reconnect). New /dashboard/flipdesk/analytics/performance dashboard (sortable + No-views-in-N-days chip + Recharts sparkline); Active tab gained Impr./CTR columns; overview gained stale-listings widget. AC said migration 00021 (stale numbering) → used 00136. Watchers come from the listings pull (getTrafficReport has no watchers) and are mirrored into watchers_count. Tenant-scoped via inventory_items.user_id join (listings has no user_id). tsc -b + eslint + deno check/lint + full build all pass.
- Timestamp: 2026-06-11
---

## US-339: Original-image + EXIF retention pipeline (forensic/provenance enabler)
- Status: COMPLETE
- Files changed: src/lib/image-utils.ts (extractExif + pure-TS EXIF/TIFF parser), src/components/submission/photo-upload.tsx (capture exif + originalFile on PhotoUploadItem), src/pages/new-submission.tsx (submit exif_metadata parallel array + optional original_images, disclosure note), src/types/database.ts (ImageExifMetadata, exif/original_storage_path/phash on SubmissionImageRow+Insert), services/edge-functions/src/routes/grade.ts (sanitizeExif + RETAIN_ORIGINAL_IMAGES gate + store exif jsonb + optional original retention + cleanup), supabase/migrations/00137_submission_image_exif.sql, src/pages/legal/privacy.tsx (EXIF/GPS disclosure)
- Notes: EXIF is read from the ORIGINAL file BEFORE compressImage's canvas re-encode strips it. Pure-TS parser (no dependency, mirrors existing getExifOrientation) reads JPEG APP1/TIFF IFD0 (make/model/software/dateTime/orientation) + ExifIFD (DateTimeOriginal/lensModel) + GPS IFD (lat/lon w/ ref signs). Non-JPEG → null (phones shoot JPEG/HEIC; HEIC not accepted). Server sanitizes/bounds EXIF (string caps, GPS range-checked) → submission_images.exif jsonb. Original retention is double-gated: client VITE_RETAIN_ORIGINALS + server RETAIN_ORIGINAL_IMAGES (default OFF, so fast compressed path is unchanged — no regression). Originals stored EXIF-INTACT (not stripped — forensic intent) in the same PRIVATE bucket + tenant-scoped owner folder, signed-URL-only access, never public/buyer-facing. Privacy disclosed in /privacy + upload step. Build/tsc/lint + deno check/lint all pass. Enables US-340/US-341.
- Timestamp: 2026-06-11
---

## US-340: Verified Capture: opt-in provenance booster + badge
- Status: COMPLETE
- Files changed: supabase/migrations/00138_verified_capture.sql (new), services/edge-functions/src/lib/verified-capture.ts (new), services/edge-functions/src/tests/verified-capture_test.ts (new), services/edge-functions/src/lib/grading-pipeline.ts, services/edge-functions/src/routes/grade.ts, services/edge-functions/src/routes/content-public.ts, src/types/database.ts, src/pages/certificate.tsx, src/pages/new-submission.tsx, src/pages/legal/privacy.tsx, functions/cert/[id].ts, prd.json
- Notes: Provenance is a POSITIVE booster, not a fraud gate. Server-side evaluateVerifiedCapture() checks an opted-in submission for consistent/recent/unedited single-device EXIF (read pre-compression in US-339) and no cross-account photo reuse (US-337). On pass: small confidence boost (capped, never lowers; respects the partial-image ceiling) + "Verified Capture" badge persisted to grade_reports.verified_capture. Missing EXIF / not opting in is never penalized. Public view + cert endpoint + SSR expose only the pass/fail boolean (raw device/recency stays server-side, like authenticity tells). Opt-in toggle in new-submission is gated on every photo carrying device+timestamp metadata. Tuning via GRADING_VERIFIED_CAPTURE_MAX_AGE_DAYS (30) / _CONFIDENCE_BOOST (0.05). Verified: deno test (9 pass), deno check, deno lint, tsc -b, npm run build, eslint.
- Timestamp: 2026-06-11
---

## US-341: Server-side forensic/ML manipulation pass on original images
- Status: COMPLETE
- Files changed: services/edge-functions/src/lib/forensics.ts (new), services/edge-functions/src/tests/forensics_test.ts (new), services/edge-functions/src/lib/grading-pipeline.ts, services/edge-functions/src/lib/schema-version.ts, supabase/migrations/00139_forensic_analysis.sql (new), src/types/database.ts, prd.json
- Notes: Decoder-free structural forensic layer (no native image codec in Deno edge): JPEG marker walk -> Adobe APP14 marker, progressive scan, DQT count + luminance quant-table quality estimate (estimateJpegQuality inverts the Annex-K scaling), plus EXIF editor-software + provenance-absent tells. Pure scoring (scoreImageForensics) is FP-guarded so NO single benign edit (re-save/Adobe export/progressive/absent-EXIF) crosses the 0.6 suspect threshold — flagging needs corroboration. aggregateForensics -> overall tamper likelihood; fuseTamperSignals combines forensic+vision via noisy-OR (corroboration => max confidence penalty 0.3 + review), and when no original was retained the fusion is exactly the vision signal (compressed-only path unchanged). Orchestration (runForensicPass) runs ONLY on retained originals (US-339 original_storage_path), bounded by FORENSIC_MAX_IMAGES (default 6) + FORENSIC_ANALYSIS kill-switch, never throws. Wired into grading-pipeline after composite: lowers confidence, sets needs_human_review, adds detailed_notes.forensic_analysis, flags moderation on forensic-suspected, persists grade_reports.forensic_analysis (migration 00139, omitted from public_grade_reports view). Pixel-domain ELA/PRNU/copy-move/inpainting deferred to a GPU/worker tier feeding the same ForensicFeatures+scoring core. Bumped EXPECTED_SCHEMA_VERSION 00135->00139 (was already stale behind concurrent 00136-00138). 21 unit tests pass; full edge suite 700+ pass; frontend tsc + build clean.
- Timestamp: 2026-06-11
---

## US-350: Run the tenant-isolation regression suite in CI
- Status: COMPLETE
- Files changed: .github/workflows/tenant-isolation.yml (new), services/edge-functions/scripts/seed-tenant-isolation-fixture.ts (new), services/edge-functions/src/tests/tenant-isolation_test.ts, prd.json
- Notes: tenant-isolation_test.ts was env-gated (ignore:!CONFIGURED) so it SKIPped in CI and a missing .eq(user_id) scoping (US-268) could ship silently. New dedicated workflow boots a LOCAL throwaway Supabase stack (supabase start applies all migrations), exports its URL/anon/service keys, runs the Deno seed script to create two confirmed users (A=victim, B=attacker) + mint password-sign-in JWTs + seed A-owned rows (inventory_item, listing, sync_run, reconcile_session, payout_import, sync_conflict, api_key, template, repricing_rule, generation_batch, grading_submission), then starts the edge service (EDGE_ENV=test keeps boot-time env validation permissive; dummy EBAY_* make isEbayConfigured() true so revise/category-check reach the ownership 404 instead of 503) and runs the suite with TENANT_ISOLATION_REQUIRED=1. New REQUIRED no-skip guard test fails the merge gate if BASE/A_JWT/B_JWT or any seeded resource id is missing (suite can't silently SKIP). Added five new cross-tenant cases per AC: revise (POST listings/:id/revise), category-check (GET listings/:id/category-check), reconciliation commit (POST reconciliation/dismiss/:id on payout_imports + POST conflicts/resolve asserting resolved=0 and A's conflict reported failed), disclosure upload (POST disclosure/item/:itemId/annotated-photo), sync-runs (GET ebay/sync-runs — A's run id never in B's list). Verified locally: deno lint + deno check clean on both new files; suite still passes with all 36 tests ignored when unconfigured; REQUIRED-mode guard correctly FAILS when fixture env absent. Frontend untouched (no tsc/build impact). Could not boot full Supabase on Windows to validate the seed against live schema; seed columns taken from confirmed migration definitions (required NOT-NULL-no-default sets only).
- Timestamp: 2026-06-11
---

## US-364: Revoke eBay tokens upstream on disconnect/deletion
- Status: COMPLETE
- Files changed: supabase/migrations/00140_marketplace_external_account_id.sql, services/edge-functions/src/lib/ebay-client.ts, services/edge-functions/src/routes/flipdesk-ebay.ts, services/edge-functions/src/routes/flipdesk-webhooks.ts, services/edge-functions/src/main.ts, services/edge-functions/.env.example, services/edge-functions/src/tests/ebay-token-revoke_test.ts, src/types/database.ts, src/hooks/use-ebay.ts, src/pages/flipdesk/marketplaces.tsx
- Notes:
  - AC1 (revoke upstream where supported): new best-effort revokeEbayUserToken() in ebay-client.ts. Gated on EBAY_TOKEN_REVOKE_URL ("where supported" — eBay does not expose RFC-7009 revocation on every keyset). Never throws; maps 2xx->revoked, 404/501->unsupported, 5xx/network->failed. Called from the new user-initiated POST /api/flipdesk/ebay/disconnect (auth + workspace middleware, tenant-scoped) which decrypts the stored refresh token, attempts revoke, then deactivates + nulls tokens. Frontend: useDisconnectEbay() hook + a Disconnect button on the Marketplaces eBay step row.
  - AC2 (match verified eBay payload, not free-text handle): added marketplace_connections.external_account_id (migration 00140) storing eBay /commerce/identity userId, captured at OAuth connect and backfilled on refresh. getUserIdentityFromToken now returns {username, externalAccountId}. The account-deletion webhook now matches on the STABLE, signature-verified userId; the guessable account_handle match is kept ONLY as a fallback for legacy rows where external_account_id IS NULL.
  - Verification: npx tsc --noEmit clean, npm run build clean, deno check + deno lint (295 files) clean, new ebay-token-revoke_test.ts (4 tests) + existing ebay tests passing.
  - Gotcha: edge supabaseAdmin is untyped (no Database generic) so the new column needed no edge type change; frontend MarketplaceConnectionRow updated for completeness. The disconnect handler never blocks local deactivation on a revoke/decrypt failure.
- Timestamp: 2026-06-11
---

## US-368: Add CAPTCHA + auth-endpoint rate limits
- Status: COMPLETE
- Files changed: supabase/config.toml, .github/workflows/db-migrations.yml, scripts/verify.mjs, src/components/auth/turnstile.tsx (new), src/lib/auth.ts, src/pages/login.tsx, src/pages/signup.tsx, src/pages/reset-password.tsx, public/_headers, .env.example, ENVIRONMENT.md, prd.json
- Notes:
  - AC1 (Turnstile/hCaptcha on signup/login/reset via GoTrue captcha config): config.toml [auth.captcha] enabled=true, provider="turnstile", secret=env(SUPABASE_AUTH_CAPTCHA_SECRET). Production self-hosted GoTrue is the enforcer (GOTRUE_SECURITY_CAPTCHA_ENABLED/PROVIDER/SECRET) — documented in ENVIRONMENT.md, mirroring the established password-policy/rate-limit local-vs-prod split. Frontend: new components/auth/turnstile.tsx renders the Cloudflare Turnstile widget (explicit render, single shared script load, single-use-token reset on retry), gated entirely on VITE_TURNSTILE_SITE_KEY so it's a no-op until provisioned. Wired into login/signup/reset-password forms; lib/auth.ts forwards captchaToken to signUp/signInWithPassword/resetPasswordForEmail.
  - AC2 (GoTrue email-send + per-IP signup rate limits): config.toml [auth.rate_limit] email_sent=4/hr, sign_in_sign_ups=30, token_verifications=30, token_refresh=150 (per 5min/IP). Prod GoTrue mirrors via GOTRUE_RATE_LIMIT_*.
  - AC3 (verified a script can't farm N trial accounts / N reset emails per minute): enforced by the GoTrue per-IP sign_in_sign_ups + per-project email_sent caps; verification procedure (loop signup/recover from one IP → HTTP 429; tokenless signup/recover → HTTP 400 captcha-disallowed) documented in ENVIRONMENT.md "Checklist after applying".
  - CSP: added https://challenges.cloudflare.com to script-src + frame-src in public/_headers (Turnstile loads a script + injects an iframe).
  - CI safety: captcha secret is env()-interpolated so no secret is committed; db-migrations.yml + verify.mjs db lane feed a dummy SUPABASE_AUTH_CAPTCHA_SECRET (mirrors the existing Google OAuth dummy) so [auth.captcha] parses. verify.mjs now also backfills the Google dummies for the local db lane (previously absent).
  - IMPORTANT coupling: prod GoTrue captcha-enabled and the frontend VITE_TURNSTILE_SITE_KEY must be flipped together or auth breaks (GoTrue rejects tokenless calls). Documented in config.toml, .env.example, and ENVIRONMENT.md.
  - Verification: npx tsc -b clean; npm run lint clean (0 errors, 1 pre-existing-style react-refresh warning matching confirm-dialog.tsx); npm run build clean (28 prerendered pages); supabase db reset re-applied all 140 migrations cleanly with the new config (CI db-migrations will pass); vitest 402/402 passing.
- Timestamp: 2026-06-11
---

## US-374: Self-serve MFA (TOTP) for all users + workspace enforcement
- Status: COMPLETE
- Files changed: supabase/migrations/00141_self_serve_mfa.sql; services/edge-functions/src/lib/recovery-codes.ts (new); services/edge-functions/src/lib/workspace-roles.ts (workspaceMfaBlocked + MFA_THRESHOLD_ROLES); services/edge-functions/src/middleware/workspace.ts (enforce owner MFA policy); services/edge-functions/src/routes/account.ts (recovery-code GET/POST/consume); services/edge-functions/src/routes/workspace.ts (GET/PUT /mfa-policy); services/edge-functions/src/tests/workspace-mfa_test.ts (new); src/components/settings/mfa-card.tsx (new); src/pages/settings.tsx; src/pages/team.tsx (WorkspaceMfaPolicyCard); src/lib/edge-fetch.ts (403 workspace_mfa_required handler); src/types/database.ts; docs/MFA.md (new)
- Notes: TOTP enrollment stays client->Supabase (supabase.auth.mfa.*) so a workspace MFA requirement can never block enrollment. Recovery codes are minted/consumed by the edge service from an AAL2 session, persisting only SHA-256 hashes; lost-device consume burns a code (atomic update-then-select to avoid double-spend) and unenrolls all TOTP factors via auth.admin.mfa.deleteFactor so the user can re-enroll. Owner policy stored on users.workspace_mfa_required_role; enforced in workspaceMiddleware via pure, unit-tested workspaceMfaBlocked(); owner never forced by own policy. listFactors().totp is verified-only in supabase-js types — use .all to reach unverified factors. Verified: tsc --noEmit, npm run build, deno check, deno lint, eslint, 9 new deno tests all green.
- Timestamp: 2026-06-11
---

## US-377: Capture explicit ToS/Privacy acceptance at signup (clickwrap)
- Status: COMPLETE
- Files changed:
  - supabase/migrations/00142_legal_acceptance.sql (new): users.{tos,privacy}_accepted_{version,at} columns; legal_acceptances audit table (RLS own-row SELECT only); handle_new_user extended to stamp acceptance from email signup metadata + append an audit row
  - src/types/database.ts: UserRow acceptance columns; LegalAcceptanceRow + legal_acceptances table entry
  - src/lib/constants.ts: LEGAL_VERSIONS (tos/privacy effective-date versions)
  - src/lib/auth.ts: signUpWithEmail passes tos_version/privacy_version/legal_accepted_at in options.data
  - src/pages/signup.tsx: affirmative clickwrap checkbox gates Create-account AND Continue-with-Google; removed passive browsewrap text
  - services/edge-functions/src/routes/legal.ts (new): GET /status, POST /accept (records audit row w/ IP+UA, updates current columns), GET /export
  - services/edge-functions/src/main.ts: mount /api/legal with authMiddleware + rate limit
  - src/components/auth/legal-gate.tsx (new): blocks dashboard until current versions accepted (OAuth first-access capture + re-acceptance on version bump)
  - src/components/auth/protected-route.tsx: wrap Outlet in <LegalGate>
- Notes:
  - Email signups are recorded server-side at account creation via the trigger (no edge round-trip). OAuth signups carry no checkbox through the redirect, so LegalGate is the authoritative capture before first dashboard access.
  - Re-acceptance: bump LEGAL_VERSIONS (constants.ts) + the TOS_VERSION/PRIVACY_VERSION mirror in legal.ts + the effectiveDate in pages/legal/{terms,privacy}.tsx — a stale recorded version re-triggers the gate for everyone.
  - Edge route is per-user scoped via c.get("userId") (never an id from the body) per the CLAUDE.md service-role rule.
  - Verified: tsc --noEmit, npm run build, npm run lint (0 errors), deno check + deno lint, supabase db reset (00142 applies clean).
- Timestamp: 2026-06-11
---

## US-378: Make onboarding essentials non-skippable + design new-user empty states
- Status: COMPLETE
- Files changed: src/stores/onboarding-tour-store.ts (new), src/components/onboarding/onboarding-flow.tsx, src/pages/settings.tsx, src/pages/inventory.tsx, prd.json
- Notes: Essential steps (verify email, accept terms) were already non-skippable via existing gates — VerifyEmailGate (US-366) and LegalGate (US-377) in protected-route.tsx. Made use_case capture non-skippable in OnboardingFlow: dialog is non-dismissible (no Escape/outside-click) until a use case is picked, and the "Skip tour" button only renders after selection, so use_case is always persisted even when the tour itself is skipped. Added useOnboardingTourStore (mirrors flipdesk-tour-store) so the welcome/product tour is re-openable from Settings via a new "Product tour" card; OnboardingFlow now also shows on `reopened`, restarting at the welcome step and seeding the saved use_case. Empty states: submissions + dashboard already had brand-new states; inventory now distinguishes "No matching items" (filters active) from the brand-new "Add your first item" CTA. Verified with tsc --noEmit, npm run build, and eslint.
- Timestamp: 2026-06-11
---

## US-380: Handle duplicate-account / identity-linking across OAuth and password
- Status: COMPLETE
- Files changed: src/lib/auth-identity.ts (new), src/lib/auth-identity.test.ts (new), src/pages/login.tsx, src/pages/auth-callback.tsx, supabase/config.toml, prd.json
- Notes: Documented GoTrue account-linking policy in config.toml — automatic identity linking on confirmed-email match is left ON (default) so Google sign-in links to an existing email/password account instead of duplicating; manual linking endpoint stays disabled (GOTRUE_SECURITY_MANUAL_LINKING_ENABLED unset). Added enumeration-safe (US-369-compatible) method-mismatch guidance: login failure message now nudges toward the original method incl. Continue with Google; auth-callback detects identity-linking/duplicate errors and shows "use your original sign-in method" instead of a raw provider error. New colocated vitest covers isIdentityLinkingError + message invariants (6 tests pass). tsc/build/eslint all clean.
- Timestamp: 2026-06-11
---

## US-403: Stop loading entire datasets into the browser on Finances
- Status: COMPLETE
- Files changed: supabase/migrations/00143_finances_dashboard.sql (new), src/lib/finances-dashboard.ts (new), src/pages/finances.tsx, src/components/finances/{financial-charts,cash-flow,time-on-market,roi-analytics,inventory-aging,grade-price-correlation,profit-table,financial-export}.tsx, prd.json
- Notes: Replaced the 4 unbounded select('*') + browser aggregation with one Postgres RPC finances_dashboard(p_period_start) that computes every summary metric and chart series server-side and returns a single compact JSON doc. Period filter is pushed into the query (sale_date >= p_period_start). For consistency/correctness all sales-based metrics are scoped to status='completed' (the canonical revenue definition) within the period — previously the charts inconsistently included cancelled/refunded sales and grade/price ignored the period entirely. Row-level drill-down lists are capped to bound payload (scatter 2000, profit rows 1000, cash-flow txns 500, stale/slow items 100), each paired with a *_total count so the UI shows accurate "showing N of M" notes. Charts became pure presentational components fed pre-aggregated props (granularity/running-balance re-bucketing stays client-side on the tiny daily series). Export no longer takes raw-row props: a 2nd RPC finances_export(start,end) returns the date-filtered transaction ledger on demand (CSV/PDF). SECURITY INVOKER keeps existing RLS (owner + workspace) intact — no new data exposed. Verified: migration applies on a fresh `supabase db reset`; seeded a sample account and confirmed RPC numbers match hand-computed values (revenue 45, costs 20, net 25, margin 55.56%, avg days 28, inventory value 80, brand profit 21); tsc -b, npm run build, and eslint all clean.
- Timestamp: 2026-06-11
---

## US-404: Paginate/summarize the items_full inventory loads
- Status: COMPLETE
- Files changed: supabase/migrations/00144_inventory_status_counts.sql (new), src/pages/flipdesk/grid.tsx, src/pages/flipdesk/analytics.tsx, src/pages/flipdesk/listings.tsx, src/components/flipdesk/item-canvas.tsx, prd.json
- Notes: Killed the three unbounded `items_full select('*')` loads flagged in the P0 audit. grid.tsx → true server-side pagination: only the current PAGE_SIZE page of a slim 10-column projection is fetched via .range(), with server-side ilike search (sanitized for the PostgREST .or grammar) and count:'exact' for the total; placeholderData:keepPreviousData for smooth paging. analytics.tsx → narrowed to a 9-column projection (no jsonb comps/measurements, no per-row photo correlated-subqueries) — it's an account-wide aggregation so pagination doesn't apply, but the slim scan is what keeps it fast. Migration 00144 adds inventory_status_counts() (language sql, stable, SECURITY INVOKER, search_path=public) returning per-status row counts as jsonb; listings.tsx tab counts now come from that grouped count query (falls back to counting loaded rows until the RPC resolves). listings.tsx select('*') → explicit projection that drops the 2 expensive per-row photo subqueries (photo_count, has_required_photos) + ai_field_sources/ai_enriched_at so Postgres can prune them; item-canvas.tsx now lazy-loads those 4 for the single open item (ai_field_sources folded into the existing color/material fetch; photo flags derived from an item_photos query). comps + measurements stay in the listings projection because listability scoring (highest_comp sort) and the editor's measurement/comp_set state need them across the loaded set — dropping measurements would also risk a save-time wipe. All items_full-derived caches keyed under the "items_full" prefix (["items_full","grid",...], ["items_full","analytics",...], ["items_full","status_counts",...]) so existing mutation invalidations of ["items_full"] still refresh them. Gotcha: separating analytics/grid into their own slim cache entries was required — they previously shared ["items_full",user.id] with listings/pipeline which expect full rows, so a slim load would have starved those of columns. Verified: npx tsc --noEmit, npm run build (tsc -b + prerender), and eslint on all four changed files clean. DB lane (supabase db reset) not run locally — Docker not started this session; migration is plain SQL mirroring the 00143 finances RPC pattern.
- Timestamp: 2026-06-11
---

## US-405: Eliminate the per-row N+1 in the eBay sync
- Status: COMPLETE
- Files changed: services/edge-functions/src/routes/flipdesk-ebay.ts, prd.json
- Notes: doListingsPull() previously did, per offer/legacy listing, a SELECT (most-recent listing) + an INSERT/UPDATE + a status flip — ~thousands of sequential PostgREST round-trips on a large seller. Refactor: (1) pre-load all of this user's existing eBay listings (most-recent per item) into an `existingListingByItem` Map via ONE inner-join query (tenant-scoped on inventory_items.user_id), replacing the per-row SELECT in BOTH the modern offers loop and the legacy Trading-API loop. (2) Accumulate every write in memory and flush as a handful of bulk calls before the orders pass: a single `listings.upsert(onConflict:"id")` over full uniform `ListingWrite` rows — new rows get client-generated `crypto.randomUUID()` ids so a later pass (and the flush) addresses the same row instead of double-inserting, and every row carries all NOT NULL columns so the upsert's INSERT arm is valid for both new and existing; a single `flipdesk_ebay_listings.upsert(onConflict:"user_id,ebay_item_id")` over an `orphanByEbayId` map (de-duped by ebay_item_id so the bulk upsert can't "affect a row a second time"); and the inventory status flips collapsed to TWO `.in("id",[...])` updates — a notify set (modern offers, fires the listing_live notification once on the real transition via `.select()`) and a silent set (legacy). Notify flip runs first so an item active on both surfaces still notifies. (3) Catalog patches deliberately kept as per-row UPDATEs: inventory_items has NOT NULL cols (status, etc.) the upsert INSERT arm would violate or clobber, and patches are gated fill-if-blank so steady-state syncs issue ~0 of them. Behavior preserved: US-148 source-of-truth pins + cross-source observations (computed from the pre-loaded snapshot before overwrite), US-465 orphan match_status preservation (column still omitted from the upsert), US-737 notifications, forward-only prep-status flips. Latent bug avoided: the old per-row INSERT spread a patch whose listing_price could be `undefined` (NOT NULL) when an offer had no price; the new ensurePendingListing defaults it to 0. Verified: `deno check src/main.ts`, `deno lint`, and the ebay-catalog-merge/ebay-order-lines/tenant-isolation/reconciliation-shape test files all green. Live-DB benchmark (AC3) not runnable here (no integration env); the round-trip count for a 1k-offer steady-state re-sync drops from ~4k to ~5.
- Timestamp: 2026-06-11
---

## US-406: Bound eBay API fan-out + honor rate limits
- Status: COMPLETE
- Files changed: services/edge-functions/src/lib/retry.ts, services/edge-functions/src/lib/ebay-client.ts, services/edge-functions/src/tests/retry_test.ts, prd.json
- Notes: Three-part fix for the listAllOffers fan-out (one getOffers per SKU, no cap/backoff). (1) Retry-After honored: ebay-client fetchAuthedOnce parses the response Retry-After header (whole-seconds or HTTP-date) via parseRetryAfter() and attaches retryAfterMs to the thrown error; withRetry (retry.ts) waits exactly that long when present (capped at maxRetryAfterMs=30s so a hostile/misconfigured header can not stall a sync), else the existing full-jitter backoff. (2) retry.ts exports isRateLimitError() (429 / quota-message only, distinct from generic 5xx) and retryAfterMs(). (3) listAllOffers bounds the SKU fan-out at MAX_OFFERS_FANOUT_PER_SYNC=2000 with OFFERS_FANOUT_CONCURRENCY=5 (tuned to Sell-API limits): when allItems exceeds the ceiling it processes the prefix and pushes a documented partial-continuation warning (next incremental sync continues from the stable inventory_item order); when a per-SKU call exhausts retries on a 429 it sets rateLimited, stops the fan-out, and pushes a rate-limit-SPECIFIC partial warning instead of silently truncating. Both warnings flow into doListingsPull errors[], which already flips the run to status partial (flipdesk-ebay.ts:2156). eBay has no bulk getOffers (fan-out is forced by the API); the bulk inventory_item listing is already paginated 100/page. Verified: deno check src/main.ts + ebay-client.ts + retry.ts, deno lint clean, retry_test.ts 11/11 pass. Frontend untouched. Live 429 exhaustion not reproducible without an eBay integration env.
- Timestamp: 2026-06-11
---

## US-407: Move long-running sync/publish/autolister to a durable job queue
- Status: COMPLETE
- Files changed: services/edge-functions/src/routes/flipdesk-ebay.ts, services/edge-functions/src/tests/publish-due-batch_test.ts, services/edge-functions/COOLIFY.md, prd.json, scripts/ralph/progress.txt
- Notes: Most of this story was already satisfied by prior durability work — sync via flipdesk_sync_runs claim + stuck-reaper (US-456), autolister via batches/jobs claim+heartbeat+reclaim cron (US-525/526), publish via per-row publish_claimed_at claim + stale reclaim (US-528). The remaining gap was AC#2: publish-due scanned up to 100 drafts serially (each = several eBay calls) inside a 240s job-lock lease, so a large backlog could blow past the lease and overlap the next tick. Fixed by bounding the scan to a small batch (publishBatchLimit(), default 15, overridable via PUBLISH_DUE_BATCH_LIMIT, clamped to 100); fetch batchLimit+1 to detect remaining backlog and report response.more; ordered by scheduled_publish_at so the oldest drains first. The 5-min cron drains larger backlogs over ticks. Also registered ebay-sync-reaper in COOLIFY.md scheduled tasks (it was mounted + in LAUNCH_CHECKLIST but absent from COOLIFY.md) so stranded running sync rows are reaped autonomously, not only on a user's next pull. Edge-only + docs change (no frontend); deno check + lint clean, 5 new unit tests pass.
- Timestamp: 2026-06-11
---

## US-408: Lazy-load recharts at the chart boundary
- Status: COMPLETE
- Files changed: src/pages/flipdesk/listing-performance.tsx, src/pages/flipdesk/analytics.tsx, src/components/flipdesk/sell-through-chart.tsx (new), prd.json, scripts/ralph/progress.txt
- Notes: The story's headline AC (Finances route initial JS drops materially) and the dashboard GradeCharts were already satisfied by US-734 (commit 52448e1) — both use React.lazy+Suspense+ChartSkeleton, confirmed recharts lands in separate async chunks not the route-entry JS. Closed the two remaining user-facing FlipDesk gaps: (1) listing-performance.tsx's 100x32 table sparkline was a single trivial line, so per AC2 ("lighter lib for simple bar/line") I replaced the Recharts LineChart with a hand-drawn inline-SVG polyline (navy if rising, muted if flat/falling; non-scaling-stroke) — recharts is now entirely gone from that route's chunk (verified 0 refs in listing-performance-*.js). (2) flipdesk/analytics.tsx's sell-through BarChart (real chart w/ axes+grid+tooltip) was extracted to components/flipdesk/sell-through-chart.tsx and lazy()+Suspense-loaded, so the page shell+table paint before recharts streams (verified page chunk has 0 BarChart refs; sell-through-chart-*.js is its own chunk). Left admin-only pages (admin dashboard/ai-models/growth/system, platform-analytics) as static recharts importers on purpose: each route is already lazy, so recharts is isolated to those super-admin chunks and never reaches entry/landing/dashboard initial JS. tsc --noEmit clean; npm run build succeeds incl. prerender.
- Timestamp: 2026-06-11
---

## US-410: Add tenant key + indexes to sales/listings
- Status: COMPLETE
- Files changed: supabase/migrations/00146_sales_listings_tenant_key.sql (new), src/types/database.ts, services/edge-functions/src/routes/account.ts, src/lib/__tests__/flipdesk-lifecycle.test.ts, prd.json, scripts/ralph/progress.txt
- Notes: sales/listings had NO tenant key (audit 2026-06-03) — per-user finances/analytics + RLS reached the owner by sub-joining inventory_items. Migration 00146 denormalizes user_id onto both tables (FK users ON DELETE CASCADE, matching the parent item), backfills from inventory_items (no orphans: inventory_item_id is NOT NULL + CASCADE), and keeps it in sync via BEFORE INSERT/UPDATE OF (inventory_item_id, user_id) trigger set_tenant_from_inventory_item (SECURITY DEFINER, copies ONLY the referenced item's owner so it can't plant cross-tenant rows; INSERT still gated by post-trigger WITH CHECK). Sets NOT NULL after backfill. Added composite indexes idx_sales_user_id_sale_date (user_id, sale_date DESC) and idx_listings_user_id_listing_status (user_id, listing_status) matching the reporting sort/filter keys. Rewrote listings/sales RLS: dropped the 8 listings + 7 sales policies from 00002/00042 (all of which EXISTS-joined inventory_items) and replaced with single per-op policies on the denormalized column via is_workspace_member_with_role(user_id, role) — which returns true for the owner too (auth.uid()=workspace_owner short-circuit), so owner-OR-member access is preserved EXACTLY (viewer=rank1 so view = old is_workspace_member; sales DELETE kept owner-only since 00042 granted no workspace delete on sales). finances_dashboard/finances_export are SECURITY INVOKER so their tenant scope now rides the denormalized-column predicate instead of the join. account.ts /export now filters listings/sales by .eq(user_id) directly (index-backed) instead of .in(inventory_item_id, itemIds). database.ts: added user_id to ListingRow/SaleRow (required) + ListingInsert/SaleInsert (optional, trigger fills). Verified: tsc --noEmit clean, npm run build (incl prerender) clean, vitest lifecycle 10/10, deno check+lint account.ts clean, and `npm run verify:db` (supabase db reset from zero, all 146 migrations) PASSED.
- Timestamp: 2026-06-11
---

## US-412: Self-host/preload the Inter font off the critical path
- Status: COMPLETE
- Files changed: index.html, src/index.css, public/_headers, public/fonts/{inter-latin,inter-latin-ext,outfit-latin,outfit-latin-ext}.woff2, prd.json
- Notes: Both Inter (font-sans) and Outfit (font-display) were loaded via a JS-injected Google Fonts <link>, so self-hosting both was required to honor the "no JS-driven font injection" criterion. Downloaded the latin + latin-ext variable woff2 binaries (weight axis 100-900, one file per family/subset) from gstatic; declared @font-face with font-display:swap in src/index.css; added <link rel=preload as=font crossorigin> for the Inter latin (regular) weight in index.html outside the prerender:head markers so it persists byte-for-byte across all prerendered routes. Removed the fonts.googleapis/gstatic preconnects, the <noscript> stylesheet, and the inline-script font injection. Added /fonts/* immutable cache headers. The inline-script CSP sha256 changed but is auto-resynced by scripts/prerender.mjs at build (verified new hash in dist/_headers). tsc --noEmit + npm run build both pass; verified preload + fonts present in dist and no remaining Google Fonts refs.
- Timestamp: 2026-06-11
---

## US-413: Guarantee resized/thumbnail images in grid views
- Status: COMPLETE
- Files changed: src/pages/submission-detail.tsx, src/pages/certificate.tsx, src/pages/admin/disputes.tsx, src/pages/admin/moderation.tsx, src/pages/admin/reviews.tsx, src/test/grid-image-lazy.test.ts, prd.json
- Notes: Cloudflare Image Resizing is disabled on the zone (memory: cf-image-resizing-disabled), so AC option (a) isn't viable — took option (b). Audited every grid/card <img> under src/. item_photos grids (photo-manager, photo-uploader, autolister, composer selection/strip) already use `thumbnail_url ?? photo_url` + loading=lazy; verified-seller storefront's `photo_url` field is actually served as thumbnail_url by content-public.ts loadStorefrontListings (line 689). The genuine gaps were five grids rendering full-res signed URLs from the submission-images bucket (which has NO thumbnails) without lazy loading — added loading="lazy" + decoding="async" so off-screen cards don't download. Hero/lightbox/client-preview imgs intentionally left eager. Added src/test/grid-image-lazy.test.ts (14 tests, source-scan guard in the style of responsive-images.test.ts). tsc/build/lint/vitest all green.
- Timestamp: 2026-06-12
---

## US-415: Paginate admin list endpoints
- Status: COMPLETE
- Files changed: supabase/migrations/00147_admin_aggregates.sql (new), src/lib/admin-aggregates.ts (new), src/pages/admin/system.tsx, src/pages/admin/users.tsx, src/pages/admin/audit-log.tsx, prd.json
- Notes: Followed the US-403/US-404 server-side aggregation pattern. New migration adds 3 SECURITY DEFINER + is_admin()-guarded RPCs (DEFINER needed because these read cross-tenant data and submission_images has no admin read policy). admin_system_metrics() collapses system.tsx's 5 unbounded select('*') fetches (users/submissions/submission_images/grade_reports/sales) into one compact jsonb summary incl. hourly/daily/error time-buckets via generate_series; client only formats labels (local time) + computes MRR from PLANS. users.tsx + audit-log.tsx now paginate server-side (.range() + count:'exact', filters/search via or/eq/gte/lte), with per-page enrichment via admin_user_list_stats(uuid[]) and distinct dropdown options via admin_audit_log_filter_options(). Gotchas: jsonb_object_agg needs a text key (cast uuid::text); wrap count(*) filter(...) in parens before ::int. Verified: tsc --noEmit clean, npm run build green, npm run verify:db (supabase db reset from zero) applied 00147 cleanly. Deferred (honest follow-up, related to US-418): submissions.tsx (cross-table sort/filter + failed/overdue full scans) and ai-models.tsx accuracy aggregation.
- Timestamp: 2026-06-12
---

## US-416: Virtualize large lists/tables
- Status: COMPLETE
- Files changed: package.json (+@tanstack/react-virtual), src/components/flipdesk/virtual-list.tsx (new), src/pages/flipdesk/autolister-queue.tsx, src/pages/flipdesk/autolister-bulk-edit.tsx, src/components/flipdesk/bulk-intake.tsx, prd.json
- Notes: Added a reusable element-scoped VirtualList (absolute-positioned rows + dynamic measureElement) for the div/li lists (autolister queue rows + bulk-intake session). The bulk-edit editable grid is a <table>, so used the padding-row technique (spacer <tr>s above/below the rendered window) which keeps table semantics and a sticky <thead> intact — applied sticky+solid bg to the th cells. Dashboard <main> is the real page scroller, so each surface gets its own bounded scroll container (max-h-[70vh]/[60vh]) for the virtualizer to measure against; window virtualization wouldn't work here. Only visible rows mount, so the queue also stops mounting a ReconcilePanel (with its own query) per off-screen row. tsc --noEmit, npm run build, and eslint all clean.
- Timestamp: 2026-06-12
---

## US-417: Add a CI bundle-size budget + verify code-splitting
- Status: COMPLETE
- Files changed: vite.config.ts (+bundleModulesManifest plugin), scripts/check-bundle-budget.mjs (new), scripts/verify.mjs, .github/workflows/ci.yml, .gitignore, prd.json
- Notes: Guard targets the EAGER chunk graph = entry chunk + transitive static imports (Vite modulepreloads). check-bundle-budget.mjs asserts gz budgets (entry <=48KB now 39.7, eager total <=215KB now 192.6/4 chunks) AND that radix Dialog/Select/Popover + @tiptap stay out of the eager graph, confirming TipTap is in a non-eager (blog-editor) chunk. radix react-slot/compose-refs allowed in entry (Button uses them on landing). Robust via a build-only Vite plugin emitting build-meta/bundle-modules.json (chunk->module ids + imports), gitignored; the prerender SSR pass is serve-mode so the plugin never fires there. Wired into ci.yml (after Build) and verify:web. Verified: tsc -b + build + lint clean; negative test (inject @tiptap into entry chunk) exits 1, restore exits 0.
- Timestamp: 2026-06-12
---

## US-418: Back analytics with DB-side aggregation
- Status: COMPLETE
- Files changed: supabase/migrations/00148_flipdesk_analytics_aggregates.sql (new), src/lib/flipdesk-analytics-server.ts (new), src/pages/flipdesk/analytics.tsx, prd.json
- Notes: Moved FlipDesk Analytics aggregation (sell-through + grading-ROI) into Postgres via two SECURITY INVOKER RPCs over the items_full view (RLS preserved, same model as finances_dashboard). flipdesk_sell_through(p_group_key,p_period_start) and flipdesk_grading_roi() return compact jsonb summary rows whose count is bounded by distinct groups/buckets, independent of inventory size. The page now re-queries per group/preset (each payload minimal) instead of pulling every items_full row and aggregating client-side. SQL faithfully mirrors the pure JS helpers (kept in flipdesk-analytics.ts as the reference + type source); median uses percentile_cont(0.5) which matches the JS even/odd median. Query keys kept under the "items_full" prefix so existing mutation invalidations refresh them. tsc/build/lint all pass. DB lane (supabase db reset) not run locally — Docker not verified this session.
- Timestamp: 2026-06-12
---

## US-419: Dedupe shared heavy TanStack queries + tune cache
- Status: COMPLETE
- Files changed: src/hooks/use-items-full.ts (new), src/pages/flipdesk/{pipeline,prep,item,overview,composer,reconciliation,listings}.tsx, src/components/flipdesk/ebay-sku-match.tsx, prd.json
- Notes: Finances + Analytics already use single-endpoint RPCs (US-403/US-418). The real dedup target was the heavy items_full read: 6 routes + ebay-sku-match each re-declared the SAME ["items_full", user.id] key with divergent queryFns (different .order) and inconsistent staleTime (5min vs 15min) — a React Query foot-gun where the first-mounted consumer wins and per-call overrides are redundant. Extracted one source-of-truth hook useItemsFull() (canonical created_at desc, select *) with deliberate staleTime 15min + gcTime 30min set in one place; all 7 consumers import it. prep sorts oldest-first client-side off the shared array. Fixed a latent collision: listings.tsx shared the bare key while projecting a narrower LISTINGS_COLUMNS subset (drops photo_count/has_required_photos/ai_*) — re-keyed to ["items_full","listings",user.id] so its partial-column cache can't starve full-row consumers (prefix still covered by existing invalidations). Removed now-unused supabase/useQuery/useAuthStore/user/ItemFullRow imports per file. tsc -b + npm run build + lint all pass.
- Timestamp: 2026-06-12
---

## US-420: Parallelize the build-time prerender
- Status: COMPLETE
- Files changed: scripts/prerender.mjs, prd.json, scripts/ralph/progress.txt
- Notes: Replaced the sequential `for...of` prerender loop with a `chunkedMap(items, limit, worker)` helper that awaits one bounded chunk before starting the next (chunked Promise.all). Concurrency defaults to 8, overridable via PRERENDER_CONCURRENCY. Added perf_hooks timing — logs total render ms, concurrency, and ~ms/route so duration stays visible as routes grow. SSR render/head-build/write are synchronous so the real wall-clock win is modest, but the structure caps in-flight work and the timing log guards against regressions. Verified via full `npm run build`: 28 routes rendered in ~297ms, head-integrity guard + CSP hash + trailing-slash 301s all still pass.
- Timestamp: 2026-06-12
---

## US-421: Stop the service worker serving the stale SPA shell over prerendered/SSR pages
- Status: COMPLETE
- Files changed: vite.config.ts, prd.json, scripts/ralph/progress.txt
- Notes: navigateFallbackDenylist rebuilt (NAVIGATE_FALLBACK_DENYLIST) from PUBLIC_ROUTES + all Pages-Function path prefixes so prerendered + dynamic routes hit the network/edge instead of the cached generic shell. globPatterns html restricted to bare "index.html" (root shell only) so the 27 per-route prerendered shells aren't precached; root shell is revisioned so deploys bust stale shells. Verified against generated dist/sw.js. Root "/" left to navigateFallback (dist/index.html is the prerendered home). tsc/build/lint clean.
- Timestamp: 2026-06-12
---

## US-422: Return real 404s for unknown routes (fix soft 404)
- Status: COMPLETE
- Files changed: public/_redirects, public/404.html, src/lib/seo/__tests__/not-found.test.ts, prd.json, scripts/ralph/progress.txt
- Notes: Replaced the soft-404 SPA fallback (/* /index.html 200) in public/_redirects. Now the known client-rendered app namespaces (/dashboard[/*], /admin[/*], /auth/*, /login, /signup, /accept-invite) rewrite to /index.html 200 so authenticated deep links + guest flows keep 200, and a final /* /404.html 404 returns a real HTTP 404 for everything else. Added a static branded public/404.html with meta robots noindex and NO inline script (CSP-safe). CF Pages precedence means Pages Functions (/cert, /verified, sitemaps) and prerendered static pages are served before these splats, so they keep their own HTML/status. Had to add explicit bare-base rules for /dashboard and /admin because the "/*" splat only matches paths with the trailing slash. Build verified: prerender still prepends the 27 trailing-slash 301s ahead of the new rules (first-match wins), dist/404.html emitted. Guard test (not-found.test.ts) locks the contract so the old soft-404 can't be reinstated.
- Timestamp: 2026-06-12
---

## US-424: Add _routes.json for Pages Function precedence
- Status: COMPLETE
- Files changed: public/_routes.json (new), scripts/smoke-functions.mjs (new), src/lib/seo/__tests__/routes-json.test.ts (new), package.json (smoke:functions script), LAUNCH_CHECKLIST.md (§6 smoke row), prd.json
- Notes: Used an allowlist-style _routes.json (include = only dynamic/data Function routes: /cert/*, /verified/*, /blog/*, /og/*, /badge/*, /slab/*, /condition-index/*, /.well-known/*, all sitemap*.xml, /rss.xml, /robots.txt, /llms.txt, /csp-report). Because include is an allowlist, the SPA shell, prerendered static HTML, and hashed /assets/* are NOT Function-routed (cannot shadow Functions). exclude lists /assets/* explicitly per AC. Cloudflare custom _routes.json overrides the auto-generated one and only supports trailing /* wildcards, so each sitemap file is listed explicitly (not /sitemap*.xml). CI guard test derives the required include pattern from every functions/*.ts file so a new Function that isn't allowlisted fails the build. smoke-functions.mjs asserts each route returns its Function-only marker (cert → Product JSON-LD) and is NOT the SPA shell; exits non-zero on failure for deploy gating.
- Timestamp: 2026-06-12
---

## US-425: Make cert SSR/SPA markup consistent
- Status: COMPLETE
- Files changed: functions/_shared/blog-render.ts (add ogType param to renderLayout + new shared certificateProductLd builder), functions/cert/[id].ts (use certificateProductLd, pass ogType="product"), src/lib/seo/json-ld.ts (certificateLd: inline Organization author + score toFixed(1) for SSR parity), src/pages/certificate.tsx (ogType="product", setMetaTag og:type "product", pass hero image + category to certificateLd), src/lib/seo/__tests__/json-ld.test.ts (image-field test + SSR<->SPA equivalence test), prd.json
- Notes: Two drifts fixed. (1) og:type was "article" (a blog type) on a Product-entity page — now "product" on BOTH the SSR Pages Function (renderLayout gained an optional ogType, default "article" so blog is unchanged) and the SPA (SEO ogType + the runtime setMetaTag override, which previously set "website"). (2) The SPA omitted images in the Product JSON-LD while SSR included the hero — SPA now passes the front (else lowest display_order) image's signed URL, mirroring the SSR hero selection; both are short-lived signed URLs (acceptable: AC asks for field parity). Eliminated the duplicated-shape drift by extracting a single source-of-truth certificateProductLd() in functions/_shared/blog-render.ts (pure, no CF globals so it's vitest-importable); the SSR builds from it and a test imports BOTH it and the SPA's certificateLd() and asserts toEqual. To make them truly identical I unified two latent differences: author is now an inline Organization (was a dangling {@id} ref on the SPA — the cert page never emits the Organization node) and the Review name uses score.toFixed(1) on both. Verified: tsc --noEmit clean; tsc -p tsconfig.functions.json clean for the two changed files (only pre-existing unrelated error in untouched condition-index/[[path]].ts, which no CI lane gates on); npm run build OK; eslint clean; vitest 435 pass with coverage above thresholds.
- Timestamp: 2026-06-12
---

## US-427: Complete OG image coverage + metadata
- Status: COMPLETE
- Files changed: src/lib/seo/public-routes.ts (ROUTE_OG_IMAGES registry + ogImageForRoute() + OG_IMAGE_WIDTH/HEIGHT/TYPE + defaults), src/prerender/head-builder.ts (per-route og:image + og:image:secure_url/type/width/height/alt + twitter:image:alt), src/components/seo.tsx (ogImageAlt prop + same image meta tags), src/components/marketing/marketing-layout.tsx (passes per-route image to <SEO>), scripts/generate-og-image.mjs (cross-platform fonts + per-route card generator), public/og-image.png (regenerated 1200x630), public/social/*.png (7 new per-route cards), public/_headers (cache rules for /social + /og-image.png), src/lib/seo/__tests__/og-images.test.ts (new guard test)
- Notes: AC1 — og-image.png confirmed 1200x630; width/height/alt/type now emitted in head-builder AND seo.tsx. AC2 — chose distinct static per-route OG images (7 high-value marketing routes) over a dynamic /og/page/:route generator; placed under /social/ rather than /og/ because /og/* is the Cloudflare Pages Functions-routed namespace (dynamic cert/blog/verified OG images) and Functions resolve before static — /social/ keeps them unambiguously static. Generator made OS-portable (Liberation Sans on Linux build image, Arial on Windows/macOS — metric-compatible so deterministic layout). Guard test ties every advertised image to a real 1200x630 PNG so we never ship a 404 og:image. AC3 — live X-card/Slack-unfurl validation is a manual post-deploy step (markup verified correct in prerendered dist HTML).
- Timestamp: 2026-06-12
---

## US-428: Add twitter:site + Organization social profiles
- Status: COMPLETE
- Files changed: src/lib/seo/social.ts (new — single source for brand handles + sameAs, config-driven, dual import.meta.env/process.env read), src/lib/seo/json-ld.ts (organizationLd: env-driven sameAs + optional contactPoint/foundingDate), src/components/seo.tsx (default twitter:site from config + twitter:creator), src/prerender/head-builder.ts (emit twitter:site/creator in prerendered head), functions/_shared/blog-render.ts (PagesEnv social vars + socialProfileUrls()/twitterSiteHandle() helpers + twitter:site in renderLayout), functions/blog/[[path]].ts (SSR twitter:site + organizationLd sameAs), functions/cert/[id].ts + functions/condition-index/[[path]].ts + functions/verified/[handle].ts (SSR twitter:site), .env.example (documented all new vars), src/lib/seo/__tests__/social.test.ts (new — 5 tests)
- Notes: The "only live URLs" AC + the prior deferral (no real handles exist in-repo) is resolved by making the whole feature CONFIG-DRIVEN rather than hardcoding fabricated profiles. The public GitHub org is always emitted (it's live); X/LinkedIn/Instagram/Crunchbase + twitter:site/creator + contactPoint + foundingDate appear ONLY when their VITE_* env var holds a real, well-formed value (URL/handle/email/ISO-date), so we never ship a sameAs/twitter:site that 404s. Same VITE_-prefixed vars feed the SPA build, the build-time prerender, AND the Cloudflare Pages SSR (Cloudflare exposes dashboard vars to both runtimes) so all three emit identical markup — mirror helpers added on the functions side. social.ts reads import.meta.env (Vite/SPA/prerender-ssrLoadModule/vitest) with a process.env fallback so it works in every runtime. Owner just sets the env vars once profiles go live — no code change. Verified: tsc -b + vite build + prerender clean, full vitest 450/450, eslint clean on all touched src + functions files.
- Timestamp: 2026-06-12
---

## US-430: Decide and scope the AI-crawler policy
- Status: COMPLETE
- Files changed: functions/_shared/seo-config.ts (split CITATION_AI_AGENTS vs TRAINING_AI_AGENTS, kept ALLOWED_AI_AGENTS as the union; populated BLOCKED_AI_AGENTS with real scrapers; trainingCrawlersAllowed() env parser; AI_CRAWLER_POLICY_NOTE; buildRobotsTxt allowTraining flag; buildLlmsTxt policyNote), functions/robots.txt.ts (reads AI_TRAINING_CRAWLERS via trainingCrawlersAllowed), functions/llms.txt.ts (emits AI_CRAWLER_POLICY_NOTE), functions/_shared/blog-render.ts (PagesEnv.AI_TRAINING_CRAWLERS), docs/AI_CRAWLER_POLICY.md (new — stakeholder decision record), .env.example (AI_TRAINING_CRAWLERS), src/test/seo-crawl.test.ts (new US-430 suite + llms policy-note test)
- Notes: Explicit decision = ALLOW training crawlers by default (public site is marketing we want memorised; nothing proprietary is crawlable — dashboard/admin/api/auth already Disallowed for all), but made CONFIGURABLE via the AI_TRAINING_CRAWLERS Pages env var so the owner can flip GPTBot/ClaudeBot/Google-Extended/Applebot-Extended to Disallow with zero code change. Citation/search bots are always allowed; aggressive non-attributing scrapers (Bytespider/CCBot/Diffbot/Omgilibot/ImagesiftBot) are always hard-blocked. The existing seo-crawl tests stay green because ALLOWED_AI_AGENTS remains the citation+training union and every welcomed bot still emits a User-agent block. Verified: tsc --noEmit clean, npm run build clean, eslint clean on touched files, vitest 17/17 on seo-crawl + llms-txt.
- Timestamp: 2026-06-12
---

## US-433: Render visible breadcrumbs + internal links
- Status: COMPLETE
- Files changed: src/components/breadcrumbs.tsx (new), src/components/breadcrumbs.test.tsx (new), src/components/marketing/marketing-layout.tsx, src/components/legal/legal-layout.tsx, src/pages/marketing/grading-glossary.tsx, src/pages/certificate.tsx, functions/_shared/blog-render.ts, functions/cert/[id].ts, functions/verified/[handle].ts, functions/blog/[[path]].ts, prd.json, scripts/ralph/progress.txt
- Notes: breadcrumbLd was emitted everywhere but no visible breadcrumb UI existed (the audit gap). Built ONE source of truth per surface so the visible nav can't drift from the JSON-LD. SPA: new <Breadcrumbs> renders the exact {name,url} trail that MarketingLayout/LegalLayout/cert page pass to breadcrumbLd() (absolute SITE_URL links → root-relative <Link> hrefs; last item is aria-current, not a link). Removed the glossary page's hand-rolled 2-level nav (the layout now renders the full 3-level trail GradeThread→Condition grading→term, matching the LD). Because the build-time prerender SSR-renders the real React tree, the visible breadcrumb appears in dist/<route>/index.html automatically — verified names+URLs == BreadcrumbList JSON-LD for /pricing and /grading/excellent. SSR (Cloudflare Pages Functions, what crawlers see for blog/cert/verified): added pure breadcrumbListLd()+renderBreadcrumbs() helpers in functions/_shared/blog-render.ts (+ breadcrumb CSS, incl. a --wide variant for .container--wide blog index/tag); each page now builds one breadcrumbItems[] trail and feeds it to both the visible <nav> and the JSON-LD. Wired into cert, verified, blog post, blog index (was no LD before), and blog tag (added LD). AC2 (pillar<->spoke + related links) was already satisfied: /condition-grading links to every glossary spoke and each spoke links back + to related terms; blog posts carry renderRelatedPosts; verified links to certs — all with canonical-path URLs. Guard test breadcrumbs.test.tsx (8 cases) pins React + SSR output to the JSON-LD trail. Verify: tsc --noEmit clean, tsc -p tsconfig.functions.json clean on touched files (only the pre-existing condition-index error remains, per US-425), npm run build OK (28 routes prerendered), eslint clean on all touched src+functions files, vitest 463/463 (was 450).
- Timestamp: 2026-06-12
---

## US-434: Enforce content image alt + dimensions
- Status: COMPLETE
- Files changed: functions/_shared/blog-render.ts, functions/blog/[[path]].ts, src/components/content/tiptap-toolbar.tsx, src/test/responsive-images.test.ts, prd.json, scripts/ralph/progress.txt
- Notes: Audit gap was blog SSR rewriteContentImages never backfilling missing alt and emitting no dimension reservation. Fix layered across three surfaces. (1) SSR render (rewriteContentImages): added deriveAltFromSrc(src) — humanizes a word-like filename ("vintage-levis-jacket.jpg" -> "vintage levis jacket"), returns "" for hash/UUID/dimension-only names so we don't surface gibberish. rewriteContentImages now: backfills alt only when absent (an explicit alt="" decorative marker is respected) using filename -> opts.fallbackAlt (post title) -> ""; reserves a guaranteed, NON-distorting aspect-ratio (style="aspect-ratio:auto 16/9" — reserves 16:9 up front but defers to the image's natural ratio once known) when the img has no explicit width+height and no existing style; still idempotent (every guard re-trips on a 2nd pass) and now strips the upstream sanitizer's void-slash so appended attrs are well-formed. New optional 3rd arg opts.fallbackAlt. (2) functions/blog/[[path]].ts renderPost passes fallbackAlt: post.title. (3) Tiptap toolbar addImage now prompts for descriptive alt on every insert (upload + URL paths) -> the "editor validation" half of AC#1; SSR fallback covers legacy/blank. Hero already satisfied AC#2 via the .hero { aspect-ratio:16/9 } CSS. Sanitizer already allowlists img width/height/alt so authored dims survive publish. Tests: +9 cases in src/test/responsive-images.test.ts (alt backfill, hash fallback, decorative-alt respect, aspect-ratio reservation, explicit-dims left alone, void-slash, deriveAltFromSrc) — 25/25 pass. Verify: tsc --noEmit clean, npm run build OK (28 routes), eslint clean (only 2 pre-existing react-refresh warnings).
- Timestamp: 2026-06-12
---

## US-436: Show distinct error states on data pages (not empty states)
- Status: COMPLETE
- Files changed: src/components/ui/error-state.tsx (new), src/components/ui/query-boundary.tsx (new), src/components/ui/__tests__/query-boundary.test.tsx (new), src/pages/submissions.tsx, src/pages/inventory.tsx, src/pages/dashboard.tsx, src/pages/finances.tsx, src/pages/flipdesk/pipeline.tsx, src/pages/flipdesk/overview.tsx, prd.json, scripts/ralph/progress.txt
- Notes: Audit gap was that data pages only branched on isLoading, so a failed fetch fell through to the empty "create your first…" state — an outage disguised as no-data. Built two shared primitives in src/components/ui/: (1) <ErrorState> — role="alert", AlertTriangle glyph, distinct heading/sub-copy, a mailto:support@gradethread.com support line, and an outline retry button wired to refetch() that disables + spins (and shows "Retrying…") while retrying is true; deliberately renders NO create-CTA. (2) <QueryBoundary> — standardizes the loading→error→empty→content branch with deliberate precedence (isError wins over isLoading and over isEmpty) so an error is never shown as empty; props map straight off useQuery (isError, onRetry=refetch, isRetrying=isFetching). Wired distinct error states into the pages the audit named: dashboard (recent-submissions card + total-submissions stat), submissions (list via QueryBoundary + disputes inline ErrorState), inventory (list via QueryBoundary), finances (page-level error guard wrapping all metric/chart sections, since each card's `?? 0` fallback otherwise renders $0 on outage), flipdesk pipeline (shared useItemsFull, error card before the kanban) and flipdesk overview (page-level error card). Each error state offers refetch(). QueryBoundary.children made optional so a boundary may render nothing on the empty branch. Tests: query-boundary.test.tsx (5 cases) covers ErrorState retry/support/disabled-while-retrying and QueryBoundary precedence (error beats empty, loading→empty→content order) — all pass. Verify: tsc --noEmit clean, npm run build OK (28 routes prerendered), eslint clean (only 2 pre-existing react-refresh warnings), 17/17 ui tests pass.
- Timestamp: 2026-06-12
---

## US-439: Fix dark-mode colors + contrast to AA
- Status: COMPLETE
- Files changed: src/index.css, src/lib/a11y/contrast.ts (new), src/lib/a11y/__tests__/contrast.test.ts (new), docs/ACCESSIBILITY_CONTRAST.md (new), src/layouts/admin-layout.tsx, scripts/ralph/verify-pastels.mjs (new guard), prd.json, scripts/ralph/progress.txt, + 120 component/page files (additive className-only dark: variants)
- Notes: Audit (2026-06-03) found ~135 light pastels with only 18 dark: variants, navy text ~1.4:1 on dark bg, and brand red ~3.3:1 as text. AC1 (pastels tokenized w/ dark counterparts): STATUS_TONE_CLASSES in constants.ts stays the canonical status map; wrote deterministic codemods that appended dark: counterparts to EVERY raw light pastel — bg-{50,100,200}->dark:bg-950/40|950/50|900/40, text-{600..900}->dark:text-{400,300,300,200}, border-{100,200,300}->dark:border-{900,800,800}; solid mid-tones (bg-*-500/600/700) intentionally excluded. Handled opacity-suffixed (bg-amber-50/50) and classNames nested inside template-literal ternaries (the first pass missed these because the backtick matcher swallowed nested quotes). Guard scripts/ralph/verify-pastels.mjs now reports 0 raw light pastels without a same-line dark counterpart. AC2 (brand-navy inverts): .prose-legal h2 -> text-foreground; all text-brand-navy on themeable surfaces got dark:text-foreground (preserving exact navy in light mode); pairs that already had a dark text override left untouched. AC3 (red/muted >=4.5:1, documented): the vibrant brand red can't be both a white-on-red surface and legible red text, so split: --color-brand-red deepened to fixed #cc1f3d (white-on CTA = 5.48:1 both themes), new inverting --color-brand-red-text and --destructive = #cc1f3d light / #fb5e78 dark give AA red text (light 5.26:1 / dark 5.56:1); dark destructive button uses shadcn's dark:bg-destructive/60 = 6.21:1 white-on. Migrated text-brand-red -> text-brand-red-text (63 spots); bg-brand-red/border-brand-red untouched. Always-dark admin sidebar (bg-brand-night) active item set to fixed text-[#fb5e78] since the inverting token would resolve to the dark red in light mode there. muted-foreground already AA (4.57 light / 7.47 dark) - left as-is, documented. Documented contrast check: docs/ACCESSIBILITY_CONTRAST.md + src/lib/a11y/__tests__/contrast.test.ts (9 assertions on measured ratios incl. a regression test that the old #f03d5f fails AA-normal as text). Verify: tsc --noEmit clean, npm run build OK (28 routes prerendered), eslint clean (2 pre-existing warnings), full vitest 485/485 pass.
- Timestamp: 2026-06-12
---

## US-440: Standardize loading skeletons with aria-busy
- Status: COMPLETE
- Files changed: src/components/ui/skeletons.tsx (new LoadingRegion + SkeletonRows), src/pages/content/{blog-list,social-list,topic-bank,knowledge,content-settings}.tsx, src/pages/flipdesk/{analytics,overview,listing-performance,reconciliation}.tsx, src/components/flipdesk/ebay-sku-match.tsx, src/pages/admin/audit-log.tsx, prd.json
- Notes: Added a reusable LoadingRegion wrapper (role=status, aria-busy=true, sr-only label) plus a generic SkeletonRows placeholder in the shared skeletons library. Replaced every bare "Loading…" string with layout-appropriate skeletons (TableLoadingSkeleton for tables, ChartSkeleton for analytics charts, a grid of Skeletons for the flipdesk pipeline, SkeletonRows for lists) wrapped in LoadingRegion. Decorative skeletons stay aria-hidden; the region announces. audit-log already had skeletons — wrapped them in LoadingRegion and changed its bare title text. tsc/build/lint all clean (only 2 pre-existing unrelated warnings).
- Timestamp: 2026-06-12
---

## US-443: Add in-shell 404s and noindex the root 404
- Status: COMPLETE
- Files changed: src/pages/not-found.tsx, src/routes/index.tsx, prd.json
- Notes: Added InShellNotFound named export to not-found.tsx using content-relative min-h-full (not min-h-screen, which double-scrolls inside the constrained <main>). Wired catch-all routes /dashboard/* (inside DashboardLayout children) and /admin/* (inside AdminLayout children, with homeTo="/admin") so unknown authed paths keep the sidebar+header. The static "dashboard"/"admin" segment outranks the bare root path:* splat, so those routes win. Root 404 already carried <SEO noindex> (criterion 2 pre-met).
- Timestamp: 2026-06-12
---

## US-444: Add inline field-level validation with ARIA
- Status: COMPLETE
- Files changed: src/lib/validation.ts (new), src/pages/login.tsx, src/pages/signup.tsx, src/pages/reset-password.tsx, prd.json, scripts/ralph/progress.txt
- Notes: Reused the existing FieldError component (src/components/ui/form-feedback.tsx) and the focus-first-invalid pattern already established in garment-info-form.tsx, rather than inventing new helpers. Added shared client validators (validateEmail, validateRequired) in src/lib/validation.ts. Auth forms (login, signup, reset-password request + update password) now: render inline errors with aria-invalid + aria-describedby that persist until the field is edited; move focus to the first invalid field on submit via document.getElementById; set noValidate on the <form> so JS validation is authoritative (native bubbles no longer pre-empt the inline errors). Password field on signup/update keeps the PASSWORD_HINT associated via aria-describedby when no error. tsc/build/eslint all clean.
- Timestamp: 2026-06-12
---

## US-445: Prevent double-submit on mutation buttons
- Status: COMPLETE
- Files changed: src/pages/accept-invite.tsx, src/pages/content/blog-list.tsx, src/pages/admin/growth/announcements.tsx, src/pages/admin/growth/campaigns.tsx, src/components/announcements/announcement-banner.tsx, prd.json
- Notes: Audited src/ for network-write buttons firing on click with no in-flight guard. Added disable-while-pending to 5 offenders: accept-invite Switch-accounts (local useState), blog-list/announcements/campaigns deletes (mutation.isPending), campaigns broadcast Send (sendingId state + early-return guard + try/finally), announcement-banner dismiss. Create/sale/payment/checkout/dialog-save buttons were already gated. tsc + build clean.
- Timestamp: 2026-06-12
---
