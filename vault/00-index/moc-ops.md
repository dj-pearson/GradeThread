---
title: 10-ops — operations and runbooks
type: moc
status: current
source_of_truth: vault
code_refs: []
reviewed: 2026-08-25
tags: [meta, moc]
summary: Map of content for 10-ops.
---

# 10-ops — operations and runbooks

Generated by `scripts/vault-index.mjs`. Reached from [[INDEX]].

<!-- vault-index:start -->

- [[admin-authority-tiers]] — contract — Three tiers of admin authority — scope only, scope plus a day-window step-up, scope plus a five-minute step-up — and the rule for deciding which an action belongs to.
- [[ai-spend-failure-domain]] — decision — What still costs money during a Postgres outage, why the correlation is accepted, and the dollar bound.
- [[backups]] — runbook — What is backed up, how often, and the restore drills that prove it works — Postgres AND the storage mirror. The storage half gained a restore script and drill in US-2659; its crypt password still lives on the host it protects against losing.
- [[billing-refunds]] — runbook — How to issue and reconcile a refund.
- [[capacity]] — runbook — Where memory and throughput limits bite, and the scale-out rule.
- [[comp-read-calibration-spike]] — runbook — How to run the US-2842 calibration spike against production, what each number it prints means, and why it stops short of a verdict.
- [[connection-pooling]] — runbook — Pool modes, limits, and which clients must use which port.
- [[connector-launch]] — runbook — The two flags that gate the connector, the order to flip them in, and the checks that prove each step before the next one.
- [[cron-schedule-governance]] — decision — The 77 production schedules live in Coolify and are governed by a manifest in the repo plus a drift check, rather than being created from code.
- [[data-retention]] — runbook — The retention schedule and purge job, plus the export and erasure paths that satisfy portability and right-to-be-forgotten.
- [[deploy]] — runbook — Ship to prod in the load-bearing order: migrations, then edge, then frontend.
- [[dns-and-routing]] — contract — Two hostnames serve two different systems; calling an app route on the Supabase host 404s silently.
- [[ebay-orders-backfill]] — runbook — How to recover eBay orders that a pre-US-2320 sync skipped past, how to tell whether a seller lost any, and why the run status field is the wrong thing to check.
- [[edge-hang-vs-crash-loop]] — runbook — Two edge failure modes with opposite signatures — a dying process that restarts itself, and a live process that never will. Telling them apart is the whole job; the hang recurred 2026-08-09 and ran far longer than the watchdog is meant to allow.
- [[edge-runtime-invariants]] — contract — The edge runs N replicas, migrations apply separately from the code roll, and a deadline must cover the response body — three facts that constrain what any edge module may assume.
- [[encryption-at-rest]] — reference — Exactly which GradeThread data is encrypted at rest, by what mechanism, and with which key reference — plus the gaps, stated plainly.
- [[env-reference]] — contract — Every env var the codebase reads, which of the eight deployment surfaces it belongs to, and which six are boot-fatal in production.
- [[extension-adapter-verification]] — runbook — How to prove a research adapter reads a real listing correctly, and how to record the result in both config files without drifting them.
- [[feature-flag-targeting]] — contract — A flag rule is resolved against the caller's EFFECTIVE plan, resolved inside isFeatureEnabled rather than supplied by the caller — and only the flags whose every call site can name a user may be plan-targeted at all.
- [[impersonation-session-revocation]] — contract — Stopping an impersonation falls back to deleting the target's auth.sessions rows through an RPC we own, because GoTrue's admin logout route does not exist on the version this project runs — and even a working revocation cannot kill an access token already issued.
- [[incident-response]] — runbook — Per-scenario engineering runbooks plus the SEV process, notification chain and monitoring thresholds.
- [[key-rotation]] — runbook — How to rotate every secret the platform holds, including the keyed dual-key path for marketplace token encryption.
- [[launch-checklist]] — runbook — The env gate, scheduled tasks, backup drill and smoke tests before going live.
- [[mfa]] — runbook — How admin step-up auth works and how to recover an operator.
- [[mfa-ipv6-ip-mismatch]] — runbook — Why admin writes 403 under IPv6 and the Kong XFF normalisation fix.
- [[migrations-process]] — runbook — How migrations are authored, verified and applied to self-hosted prod.
- [[postgrest-row-cap]] — contract — There is NO row cap in prod (db-max-rows is unset, read from pg_roles) — the bound that actually bites is an 8s statement_timeout, 3s for anonymous; every read must still page until empty, count without rows, or aggregate in SQL.
- [[priority-5-operator-queue]] — runbook — The priority-5 backlog is almost entirely code-complete and waiting on production actions only the owner can take; this is that queue, grouped by what you have to open.
- [[qa-photo-access]] — runbook — How QA reaches submission images without breaking the private-bucket rule.
- [[reading-prod-schema-without-psql]] — runbook — PostgREST publishes an OpenAPI document listing every exposed column, its nullability and every RPC - so a whole class of "go and run this in psql" operator steps can be answered from a laptop with the public anon key.
- [[rollback]] — runbook — Undo a bad release on each surface, and what cannot be rolled back.
- [[runbook-copies]] — reference — Ops procedures are duplicated across repo root, docs/, and a shipped in-app admin feature — and the in-app copy is the one on-call actually reads.
- [[scaling]] — runbook — Where the platform runs out of headroom first and what to turn up.
- [[session-expiry-and-refresh]] — runbook — "Session expired" about an hour in is a refresh that failed to recover, not an expiry policy — and the durable fixes are prod GoTrue env vars this repo cannot reach.
- [[staging]] — runbook — What staging is, what it is not, and how to smoke it.
- [[system-settings]] — contract — A typed key→jsonb registry read through a caching helper, editable by a super-admin without a deploy — which is the point and also the risk.
- [[uptime-monitoring]] — runbook — The external checks, what they probe and where they alert.

<!-- vault-index:end -->
