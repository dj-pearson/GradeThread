# US-264: CSP is now ENFORCING (was Report-Only). The one inline <head> script
# (Consent Mode + font + deferred GA bootstrap in index.html) is allowed by its
# sha256 hash below — NOT 'unsafe-inline'.
#
# US-358: that hash is now AUTO-SYNCED at build time. scripts/prerender.mjs
# (runs after `vite build`) recomputes the sha256 of the BUILT inline script and
# rewrites this directive in dist/_headers, so editing the bootstrap can no
# longer silently break the CSP — no manual hash recompute needed. The value
# committed here is a placeholder kept current on the next build; the build
# fails loudly (scripts/csp-hash.mjs) if the directive's sha256 token is missing.
#
# Violations are reported to /csp-report (Pages Function) — watch the Pages logs
# after deploy and smoke-test Stripe Checkout, Google OAuth, Sentry, PostHog,
# and GA. A missed origin is a one-line add to the directive below.
# Permissions-Policy: camera=(self), NOT camera=().
#
# An empty allowlist blocks EVERY origin INCLUDING our own. This app has two
# first-party camera features: CameraCaptureDialog
# (src/components/submission/camera-capture-dialog.tsx, used by photo-upload.tsx
# — the grading photo flow) and BarcodeScannerDialog
# (src/components/flipdesk/barcode-scanner-dialog.tsx, used by intake.tsx).
# Shipping camera=() made getUserMedia reject in production and both features
# silently fail. The capture dialog's own feature-detect cannot catch this: the
# API is present, only the permission is denied, so it looks supported until the
# user taps. microphone/geolocation stay fully closed — nothing in the app asks
# for them, so keep those denials explicit.
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(self), microphone=(), geolocation=()
  Strict-Transport-Security: max-age=63072000; includeSubDomains; preload
  Cross-Origin-Opener-Policy: same-origin
  Reporting-Endpoints: csp-endpoint="/csp-report"
  Link: </fonts/inter-latin.woff2>; rel=preload; as=font; type=font/woff2; crossorigin
  Content-Security-Policy: default-src 'self'; script-src 'self' 'wasm-unsafe-eval' 'sha256-79rFJF9W1WmuW2ynG7+8bgWSgcgbohhmRmGDEV6pttw=' https://js.stripe.com https://www.googletagmanager.com https://googleads.g.doubleclick.net https://www.googleadservices.com https://us-assets.i.posthog.com https://static.cloudflareinsights.com https://challenges.cloudflare.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data: blob: https://gradethread.com https://*.gradethread.com https://*.googleusercontent.com https://*.ebayimg.com https://*.ebaystatic.com https://*.stripe.com https://www.googletagmanager.com https://*.google-analytics.com https://*.g.doubleclick.net https://pagead2.googlesyndication.com https://www.google.com; media-src 'self' blob:; font-src 'self' https://fonts.gstatic.com data:; connect-src 'self' https://api.gradethread.com wss://api.gradethread.com https://functions.gradethread.com https://staticimgly.com https://*.ebayimg.com https://*.stripe.com https://*.ingest.sentry.io https://*.ingest.us.sentry.io https://us.i.posthog.com https://us-assets.i.posthog.com https://www.google-analytics.com https://*.google-analytics.com https://*.analytics.google.com https://cloudflareinsights.com https://challenges.cloudflare.com https://pagead2.googlesyndication.com https://googleads.g.doubleclick.net https://ad.doubleclick.net https://www.google.com; frame-src https://js.stripe.com https://hooks.stripe.com https://challenges.cloudflare.com; worker-src 'self' blob:; manifest-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; report-uri /csp-report; report-to csp-endpoint

/assets/*
  Cache-Control: public, max-age=31536000, immutable

# US-412: self-hosted fonts have stable, version-pinned filenames — cache them
# for a year. A font update ships under a new filename, so immutable is safe.
/fonts/*
  Cache-Control: public, max-age=31536000, immutable

# US-427: per-route social share images (/social/*.png) and the default
# /og-image.png are not content-hashed (the filename is stable so the OG <meta>
# URL stays put), so cache for a day with background revalidation instead of
# immutable — a brand/tagline regen then propagates within ~24h.
/social/*
  Cache-Control: public, max-age=86400, stale-while-revalidate=604800
/og-image.png
  Cache-Control: public, max-age=86400, stale-while-revalidate=604800

# US-575: the SPA shell (HTML) must always be revalidated so an open tab is not
# stranded on a stale asset manifest after a deploy. The hashed /assets/* files
# above stay immutable; only the entrypoint HTML is forced to revalidate.
/
  Cache-Control: public, max-age=0, must-revalidate
/index.html
  Cache-Control: public, max-age=0, must-revalidate
